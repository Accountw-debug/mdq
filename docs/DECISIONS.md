# Entscheidungslog

Format: `D-NNN · Datum · Entscheidung · Grund · Verworfene Alternativen`

- **D-001 · 2026-08-30 · Logik und Code strikt trennen.** `logic/` (Schema, Regeln, Mappings, Wörterbücher) ist technologieunabhängig; `engine/` und `ui/` hängen daran, nicht umgekehrt. Grund: Neubau mit anderer Technologie soll nur die Engine kosten. Der ursprüngliche Wortlaut „austauschbar" ist durch D-063 überholt – die Trennung bleibt, der Prototyp-Status entfällt. Alternative verworfen: Regeln im Python-Code.
- **D-002 · 2026-08-30 · Regeln sind SQL auf dem kanonischen Schema, ausgeführt in DuckDB.** Grund: SQL ist die portabelste Logik (HANA, Databricks, Postgres). Alternative verworfen: Pandas-Pipelines, Python-Prädikate.
- **D-003 · 2026-08-30 · Dateibasierte Extraktion (SE16N/FBL-Exports) für V1.** Grund: kein Connector-Betrieb, kein Transport beim Kunden, passt zu On-Prem. RFC/OData erst mit Monitoring.
- **D-004 · 2026-08-30 · Stufenmodell A/B/C/decision mit Präzisionszielen 99/90 %.** Grund: Abdeckung über Quellen, nie über niedrigere Schwellen. Alternative verworfen: eine globale Konfidenzschwelle.
- **D-005 · 2026-08-30 · Schadensklasse 1 (Bankdaten) wird nie Stufe A.** Grund: falsche IBAN = Fehlüberweisung, Betrugsvektor. Keine Ausnahme per Policy.
- **D-006 · 2026-08-30 · Prototyp-Engine darf später neu gebaut werden.** `logic/`, `testdata/` und das UI-Design bleiben. Grund: Victor baut solo mit Claude Code vor; Lux baut die Produkt-Engine. **Abgelöst durch D-063 (2026-08-30).**
- **D-007 · 2026-08-30 · UI liest ausschließlich Findings-JSON.** Grund: UI und Engine parallel entwickelbar, Vertrag ist das Schema.
- **D-008 · 2026-08-30 · Keine echten Kundendaten im Repo, nie.** Nur Demo-Mandant und Encoding-Samples. `data/` und `runs/` ignoriert.
- **D-009 · 2026-08-30 · Geldbeträge als DECIMAL(15,2), Schlüssel als Text mit führenden Nullen.** Grund: Float-Rundung und ALPHA-Konvertierung sind die häufigsten Fehlerquellen bei SAP-Daten.
- **D-010 · 2026-08-30 · Golden Dataset als Regression.** Demo-Mandant + `testdata/expected/` sind Spec und Test; erwartete Ergebnisse werden nie an Code angepasst.
- **D-011 · 2026-08-30 · Paketlayout: `engine/mdq/` wird per hatchling als Top-Level-Modul `mdq` gebaut** (`[tool.hatch.build.targets.wheel] packages = ["engine/mdq"]`, Entry Point `mdq = "mdq.cli:app"`). Grund: Import bleibt `import mdq`, der Ordner `engine/` bleibt aber als eigene Schicht erkennbar (Wortlaut „austauschbarer Prototyp" durch D-063 überholt). Verworfen: `src/`-Layout (widerspricht der Ordnerrolle aus CLAUDE.md), `import engine.mdq` (Engine-Name im Import zementiert).
- **D-012 · 2026-08-30 · Pfade zu `logic/` als Konstanten in `mdq/__init__.py`** (`PROJECT_ROOT`, `LOGIC_DIR`, `CANONICAL_SCHEMA`, `FINDING_SCHEMA`, `RULES_DIR`), abgeleitet aus `__file__`. Grund: Regeln, Schema und Findings müssen unabhängig vom Arbeitsverzeichnis gefunden werden, sonst hängt Determinismus am Aufrufort. Verworfen: relative Pfade ab CWD, Umgebungsvariable.
- **D-013 · 2026-08-30 · Noch nicht implementierte CLI-Befehle brechen mit Exit-Code 2 und Verweis auf die zuständige Aufgabe ab.** Grund: Regel „nichts wird stumm verworfen" gilt auch für die Oberfläche – ein Stub darf keinen leeren Erfolg melden. Verworfen: Befehle erst später anlegen (CLI-Vertrag wäre unsichtbar).
- **D-014 · 2026-08-30 · Typer-Parameter werden als `Annotated[...]` deklariert, nicht als Default-Wert.** Grund: Ruff meldet Funktionsaufrufe in Default-Argumenten (B008); `Annotated` ist die vorgesehene Typer-Schreibweise und vermeidet ein `noqa`. Zusätzlich `known-first-party = ["mdq"]` für die Import-Sortierung. Verworfen: Regel unterdrücken.
- **D-015 · 2026-08-30 · Validierungsmeldungen nennen nur Pfad und verletzte Bedingung, nie den Wert.** Beispiel: `entity.bp_key: verletzt pattern: "^[CV]:[A-Za-z0-9]+$"`. Grund: die Standardmeldungen von `jsonschema` setzen die Instanz in den Text – bei `STCEG`, `display_name` oder IBAN waeren das Geschäftspartnerdaten in Logs (Regel 8). Erlaubt bleiben Feldnamen, Schlüssel und Dateinamen. Verworfen: Meldungen nachträglich maskieren.
- **D-016 · 2026-08-30 · Fehlerliste ist nach (JSON-Pfad, Validator) sortiert.** Grund: `iter_errors` garantiert keine Reihenfolge; ohne Sortierung wäre die Ausgabe nicht deterministisch (Regel 9). Verworfen: Reihenfolge von jsonschema übernehmen.
- **D-017 · 2026-08-30 · Findings werden aus YAML und JSON gelesen.** Grund: die fachlichen Beispiele sind YAML, der Vertrag zur UI ist JSON (D-007); ein Loader für beides vermeidet zwei Pfade. Verworfen: nur YAML.
- **D-018 · 2026-08-30 · Eigener `date-time`-Checker mit `datetime.fromisoformat` statt Zusatzpaket.** Grund: der `FormatChecker` von `jsonschema` prüft `date-time` nur mit `rfc3339-validator`; ohne das Paket blieb das Format still ungeprüft – genau die stille Lücke, die Regel 4 verbietet. Verworfen: `rfc3339-validator` als Abhängigkeit (Stack-Erweiterung für einen Zeilenumfang von drei Zeilen).
- **D-019 · 2026-08-30 · YAML wird mit einem SafeLoader ohne Timestamp-Resolver gelesen.** Grund: PyYAML macht aus `data_as_of: 2026-08-28` sonst ein `date`-Objekt; die Schema-Meldung lautete dann „ist kein String" statt das Format zu prüfen. So verhält sich YAML wie JSON und `format: date` greift. Verworfen: Anführungszeichen per Konvention verlangen.
- **D-020 · 2026-08-30 · `mdq validate` prüft zusätzlich auf doppelte `finding_id` im Verzeichnis** und trennt die Exit-Codes: 0 alles valide, 1 ungültig oder doppelt, 3 nichts zu prüfen. Grund: die finding_id ist ein Hash über Regel und Ist-Zustand – Duplikate bedeuten Kopie oder falsch gebildete ID; ein leerer Lauf darf nie als Erfolg durchgehen. Verworfen: Duplikatprüfung erst in der Engine.
- **D-021 · 2026-08-30 · Leere Testfälle im Regelkopf sind bis Sprint 2 eine Warnung, danach ein Fehler.** Der Loader prüft die Struktur von `tests` (hits/no_hits/edge vorhanden, Listen von bp_key-Texten) und meldet leere `hits`/`no_hits` als Hinweis mit Regel-ID in `mdq rules list`. Grund: `logic/rules/README.md` verlangt je einen Testfall, aber der Demo-Mandant entsteht erst in Sprint 2 – Testfälle jetzt zu erfinden hieße, erwartete Ergebnisse ohne Datengrundlage zu setzen (Regel 1). Verworfen: sofort harter Fehler (macht die drei vorhandenen Regeln ungültig), Prüfung ganz weglassen (Hinweis würde vergessen).
- **D-022 · 2026-08-30 · Der Regel-Loader prüft vier Invarianten, nicht nur die eine aus der Spec.** Neben `damage_class == 1 → default_tier != 'A'` auch: Schadensklasse 1 nie `mass_change_eligible`, `default_action_type == 'mass_change' → default_tier == 'A'`, und `id` muss zu Dateiname, `side` und `category` passen. Grund: dieselben Invarianten prüft das Finding-Schema erst im Lauf; im Kopf geprüft treffen sie den Regelautor sofort. Verworfen: nur die Spec-Invariante prüfen.
- **D-023 · 2026-08-30 · Alle Kopf-Probleme einer Regel werden gemeinsam gemeldet, nicht nur das erste.** Unbekannte Kopf-Felder sind ein Fehler (Regel 4). Grund: eine Regel soll in einem Durchgang korrigierbar sein. Analog zu D-015/D-016 bei den Findings.
- **D-024 · 2026-08-30 · Dateien mit Präfix `_` in `logic/rules/` sind Vorlagen und werden übersprungen, nicht milder geprüft.** `_TEMPLATE.rule.sql` würde einzeln geladen abgelehnt (Platzhalter-ID, Platzhalter-Klartext). Grund: eine Ausnahme beim Einsammeln ist nachvollziehbar, eine Ausnahme in der Prüflogik wäre ein Loch. Verworfen: Template über eine Sonderregel im Validator zulassen.
- **D-025 · 2026-08-30 · UI wird in einem eigenen Git-Worktree `../mdq-ui` auf Branch `ui-proto` gebaut.** Die UI-Session ändert ausschließlich `ui/` und führt ihre Notizen in `ui/NOTES.md`; `docs/DECISIONS.md` und `docs/SESSION_LOG.md` bleiben allein auf `main`. Beim Merge übernimmt Victor die Einträge aus `ui/NOTES.md` nach `docs/DECISIONS.md` und vergibt dort die endgültigen D-Nummern. Grund: zwei Sessions parallel ohne Konflikte in genau den beiden Dateien, die die Projekthistorie tragen; der Vertrag zwischen beiden bleibt `logic/finding.schema.json` (D-007). Verworfen: zweiter Klon (uv-Umgebung und Git-Historie doppelt), gemeinsames Arbeitsverzeichnis (Konflikte in DECISIONS/SESSION_LOG bei jedem Commit).
- **D-026 · 2026-08-30 · Beträge kommen als `Decimal` aus DuckDB und werden mit zwei Dezimalen zu Text formatiert, nie über float.** Ein float im SQL-Ergebnis ist ein Fehler, ebenso mehr als zwei Nachkommastellen – Runden gehört in die Regel, nicht in die Engine. Vorzeichen bleiben erhalten. Grund: Regel 2; stilles Runden in der Engine wäre eine unsichtbare Betragsänderung. Verworfen: `round()` im Executor.
- **D-027 · 2026-08-30 · `finding_id` = `F-` + sha1(rule_id | bp_key | company_code | source_table | source_field | current_value | finding_key)[:12]**, `|` als Trenner, NULL als leerer Text. Gegenüber der ursprünglichen Formel in SPRINT-1.md sind `company_code` und die neue optionale Ausgabespalte `finding_key` ergänzt. Grund: eine Regel kann mehrere Findings je Geschäftspartner liefern – AR-CON-002 je Buchungskreis, AP-LEA-001 je Belegpaar; ohne die beiden Bestandteile kollidieren drei Belegpaare mit identischer Referenz zu einer einzigen ID. Eine Kollision innerhalb eines Laufs bleibt ein Fehler mit dem Hinweis auf die fehlende `finding_key`-Spalte. `logic/rules/README.md` und `docs/specs/SPRINT-1.md` sind entsprechend korrigiert. Verworfen: Kollision still zur ersten Zeile auflösen, laufende Nummer anhängen (nicht stabil über Läufe).
- **D-028 · 2026-08-30 · Laufkopfdaten kommen aus einem `RunContext`, nie aus `datetime.now()` im Executor.** `run_id`, Versionen, `data_as_of` und `created_at` werden von außen gesetzt. Grund: Determinismus (Regel 9) ist mit einer Uhr im Executor nicht haltbar. Verworfen: Zeitstempel im Executor erzeugen.
- **D-029 · 2026-08-30 · Der Regel-Loader prüft auch die erlaubten Schlüssel in `remediation`.** Erlaubt sind `sap_transaction`, `path`, `field`, `mass_change_eligible`, `steps` – dieselben wie im Finding-Schema. Grund: sonst fällt ein Tippfehler erst zur Laufzeit als Schema-Fehler auf, statt beim Regelautor. Gleiche Begründung wie D-022.
- **D-030 · 2026-08-30 · Relevanzbeträge stehen in Hauswährung und werden in V1 nicht umgerechnet.** Die Felder im Finding heißen `relevance.open_items` und `relevance.volume_12m` (vorher `_eur`), dazu das Pflichtfeld `relevance.currency`; `bp_relevance` bekommt ebenfalls eine Spalte `currency`. Kommen im Scope eines Laufs mehrere Hauswährungen vor, bricht der Lauf mit klarem Fehler ab und verweist darauf, den Lauf je Hauswährung getrennt zu starten. Grund: die alten Feldnamen behaupteten EUR, während die kanonische Tabelle die Hauswährung führt – bei einem Mandanten mit CHF- oder USD-Buchungskreis wären das stillschweigend falsch etikettierte Beträge (Regel 2: Währung steht immer neben dem Betrag). Umrechnung über TCURR ist V2. Verworfen: implizit EUR annehmen, Beträge ohne Kurs-Herkunft umrechnen.
- **D-031 · 2026-08-30 · Encoding-Erkennung ohne Ratebibliothek, in fester Reihenfolge:** BOM (UTF-8-BOM, UTF-16-LE/BE) → UTF-8 strikt versucht → CP1252 als Fallback. Nur der Fallback rät; er wird als Warnung im `LoadResult` geführt und im Run-Report gezeigt. Grund: `chardet`/`charset-normalizer` entscheiden statistisch und damit nicht deterministisch (Regel 9); zudem bliebe eine geratene Annahme unsichtbar. Verworfen: Ratebibliothek, Encoding als Pflichtparameter beim Aufruf.
- **D-032 · 2026-08-30 · Alle Dateien werden über eine temporäre UTF-8-Fassung importiert.** DuckDB liest CP1252 und UTF-16 nicht; ein einheitlicher Weg für alle Encodings garantiert, dass die vier KNA1-Samples zellengleiche Tabellen ergeben. Die Rohdatei bleibt unverändert (Regel 3), und `sha256` wird über die **Originalbytes** gebildet – sonst wäre der Hash kein Beleg für die Eingabedatei. Verworfen: Encoding an DuckDB durchreichen (nur teilweise unterstützt, unterschiedliches Verhalten je Encoding).
- **D-033 · 2026-08-30 · Rohtabellen sind `raw_<TABELLE>` mit ausschließlich TEXT-Spalten** (`all_varchar`). Grund: Typ-Inferenz frisst führende Nullen in `KUNNR`/`LIFNR`/`BELNR` (Regel 2, D-009). Typisierung passiert erst beim Staging. Leere Zellen werden NULL – auch als `""` quotierte.
- **D-034 · 2026-08-30 · Spaltennamen werden gegen die Kopfzeile der Datei geprüft, nicht gegen das Ergebnis von DuckDB.** DuckDB hängt an doppelte Spaltennamen still ein `_1` an; das wäre ein stumm verworfener Unterschied (Regel 4). Leere oder doppelte Spaltennamen sind ein Fehler mit Namen.
- **D-035 · 2026-08-30 · SAP-Initialwerte werden NULL, kein Reject:** leere Zelle, Datum `00000000`/`00.00.0000`/`0000-00-00`, leerer Betrag. `"0,00"` ist dagegen ein echter Betrag. Erkannt werden deutsche (`1.234,56`) und ISO-Schreibweise (`1234.56`), Leerzeichen als Tausendertrenner sowie vorangestelltes und nachgestelltes Vorzeichen. **Ein einzelner Trenner mit genau drei folgenden Ziffern (`1.234`) ist mehrdeutig** – deutsch 1234, ISO 1,234, und bei dreistelligen Währungen (BHD, KWD) beides plausibel – und wird als `ParseError` abgelehnt statt geraten. Auf Wert-Ebene bleibt es dabei. **In echten SE16N-Exporten ist die Notation pro Datei einheitlich** – sie folgt der SAP-Benutzereinstellung (SU3, Dezimaldarstellung). Sprint 3 ermittelt die Notation je Datei aus den eindeutigen Werten (jene mit beiden Trennern oder mit ein bis zwei Nachkommastellen) und wendet sie auf mehrdeutige wie `1.234` an. Enthält eine Datei keinen eindeutigen Wert, entscheidet der Laufparameter `--decimal-notation de|iso`; fehlt auch der, bleibt es beim Reject. `testdata/encoding_samples/BSID_formats.txt` mischt beide Schreibweisen bewusst und bleibt Parser-Test auf Wert-Ebene – als Export wäre die Datei nicht realistisch (vermerkt in `testdata/README.md`).
- **D-036 · 2026-08-30 · `ParseError`- und `LoaderError`-Meldungen beschreiben die Form des Wertes, nie den Wert.** Der Rohwert gehört ausschließlich in `reject.raw_excerpt` (dort auf 200 Zeichen gekürzt) – laut kanonischem Schema die einzige Stelle für Rohdaten. Grund: Regel 8; ein Betrag oder Name kann zu einem Geschäftspartner gehören. Gleiche Linie wie D-015.
- **D-037 · 2026-08-30 · Der Run-Report ist ein Datenobjekt; die Darstellung ist davon getrennt.** `RunReport` sammelt (`add_load`, `add_rule`, `rejects`), `render()` schreibt Text, `to_dict()` liefert dieselben Zahlen als JSON. Grund: Sprint 3 braucht die Kennzahlen als JSON neben dem Lauf, und die UI liest ausschließlich JSON (D-007) – sie darf sich nie auf geparsten Terminaltext stützen. Die Anzeige kürzt den `sha256` auf 8 Zeichen, `to_dict()` behält die volle Summe. Verworfen: nur Textausgabe, Zahlen später aus dem Text zurückgewinnen.
- **D-038 · 2026-08-30 · Ein Lauf mit Rejects, übersprungenen oder fehlgeschlagenen Regeln ist kein stiller Erfolg.** `RunReport.has_problems` ist dann wahr, der Report weist übersprungene Regeln getrennt aus („N Regeln nicht geprüft", CONCEPT.md Block 3) und nennt sie mit Grund. Auch ein sauberer Lauf sagt ausdrücklich „keine Rejects", statt den Abschnitt wegzulassen. Welchen Exit-Code `mdq run` daraus macht, entscheidet Sprint 3. Verworfen: leere Abschnitte ausblenden (Abwesenheit wäre nicht von Nichtprüfung unterscheidbar).
- **D-039 · 2026-08-30 · Der Report zeigt `reject.raw_excerpt` nie an.** Er nennt Stufe, Anzahl und Grund; der Rohtext bleibt in der Tabelle. Grund: Regel 8 – ein Report wird in Tickets und Mails kopiert. Gleiche Linie wie D-015 und D-036.
- **D-040 · 2026-08-30 · `mdq load --input <verzeichnis>` als Zwischenstand.** Liest die Exporte eines Verzeichnisses und zeigt den Report; die Ausgabe kennzeichnet sich selbst als Zwischenstand, weil Mapping und Regelausführung erst in Sprint 3 kommen. Fester `run_id` `load-zwischenstand` ohne Zeitstempel, damit die Ausgabe deterministisch bleibt (Regel 9). Grund: Aufgaben 5 und 6 sind damit von Hand prüfbar, ohne auf Sprint 3 zu warten.
- **D-041 · 2026-08-30 · `title` ist Pflichtfeld im Finding-Schema und im Regelkopf.** Jedes Finding trägt eine Kurzüberschrift; die Engine füllt sie immer aus dem Regelkopf (`_fill` mit den `params` der Zeile). Damit das nicht erst zur Laufzeit auffällt, ist `title` zugleich Pflichtfeld im Regelkopf – gleiche Linie wie D-022/D-029: ein fehlender Titel trifft den Regelautor, nicht den Lauf. Zusätzlich `minLength: 1` im Schema, weil ein Pflichtfeld mit leerem Text ein Titel wäre, der keiner ist (Regel 4). Verworfen: `title` optional lassen und in der UI aus `rule_id` bauen (die UI liest ausschließlich das Finding, D-007).
- **D-042 · 2026-08-30 · Zahlungsbedingungen ZB01 und ZB02 getauscht: ZB02 ist "14 Tage 2 %, 30 netto", ZB01 "10 Tage 3 %, 30 netto".** Die erste Fassung von SPRINT-2.md ordnete die 2-%-Bedingung ZB01 zu, das Beispiel-Finding F-005 nennt aber an zwei Stellen ZB02 mit 14 Tagen und 2 %. Erwartete Ergebnisse werden nicht an den Code angepasst (Regel 1), also folgt die Spec dem Beispiel: `docs/specs/SPRINT-2.md` ist in der T052-Zeile und in der Ankertabelle (V:0000200117 → ZB02) korrigiert. Entschieden von Victor. Verworfen: F-005 auf ZB01 ändern.
- **D-043 · 2026-08-30 · Das Postenfenster endet am Datenstand (2024-09-01 bis 2026-08-28).** SPRINT-2.md nannte Posten bis 2026-08-31 bei Datenstand 2026-08-28; ein Export kann keine Belege nach dem Ziehungsdatum enthalten. `WINDOW_END` und `DATA_AS_OF` sind derselbe Tag, ein Test prüft, dass kein Buchungsdatum darüber liegt. Entschieden von Victor. Verworfen: Datenstand auf 2026-08-31 heben (widerspräche `data_as_of` in allen sechs Beispiel-Findings).
- **D-044 · 2026-08-30 · Das Zahlverhalten hängt am Geschäftspartner, nicht am einzelnen Beleg.** Jeder Partner bekommt ein Zahlerprofil (40 % skontotreu, 45 % pünktlich netto, 15 % schleppend); der Verzug je Beleg streut normalverteilt um das Profil. Grund: bei einem Münzwurf je Rechnung verteilte sich der entgangene Skonto gleichmäßig über alle Kreditoren mit Skontobedingung, und AP-LEA-002 (Meldegrenze 1.000,00 in 12 Monaten) träfe einen großen Teil des Basis-Mandanten, ohne dass ein Defekt gesetzt wäre. Entschieden von Victor.
- **D-045 · 2026-08-30 · Der Basis-Mandant ist regelfrei, und das wird geprüft.** Auf den Daten aus Aufgabe 1 darf keine Regel aus `CATALOG.md` greifen; jedes Finding muss später genau einem Defekt zuzuordnen sein. Dafür sind im Generator Invarianten eingebaut und in `engine/tests/test_demo_base.py` getestet: eindeutige Kernnamen (ohne Rechtsform), eindeutige USt-IdNr. und IBAN, Zahlweg nie ohne Bankverbindung, `ZTERM`/`MAHNA`/`REPRF` immer gesetzt, keine Löschvormerkung, **kein Belegpaar im Muster von AP-LEA-001** (zwei Rechnungen desselben Partners mit gleichem Betrag binnen 60 Tagen – der Betrag wird sonst neu gezogen) und **Skontoverlust je Kreditor unter 500,00** (der Zahler zieht das Skonto ausnahmsweise doch, bevor die Meldegrenze fällt). Verworfen: Zufallstreffer erst in Aufgabe 2 über die erwartete Findings-Liste ausgleichen – das wäre ein Testergebnis, das an die Daten angepasst wird.
- **D-046 · 2026-08-30 · Die Spaltenlisten der Exportdateien kommen ausschließlich aus `logic/mappings/sap_ecc.yaml`.** Der Generator führt keine zweite Feldliste; `writers.columns_for` liest die `fields` der Tabelle und stellt `MANDT` voran – die einzige Zusatzspalte, und im Mapping unter `ignore` bekannt. Dafür wurde `T052U` im Mapping als eigene Tabelle ergänzt (Ziel `payment_terms.description`); bisher stand sie nur unter `detect` und als `join` bei T052, hätte also eine Feldliste im Code erzwungen. Fehlt in einer Zeile eine Spalte, ist das ein Fehler und kein leeres Feld (Regel 4).
- **D-047 · 2026-08-30 · Jede Generator-Phase zieht ihren eigenen Zufallsstrom** (`random_for(seed, label)`, abgeleitet über sha1 statt `hash()`, das je Prozess anders ausfällt). Grund: mit einem gemeinsamen Strom verschöbe eine neue Phase – etwa die Defekt-Schicht in Aufgabe 2 – sämtliche Daten dahinter, und jeder Ankerwert müsste neu gesucht werden. Verworfen: ein globaler Strom, `random` ohne eigene Instanz.
- **D-048 · 2026-08-30 · Prozentfelder tragen drei Nachkommastellen; ihre Notation kommt aus dem Feld, nicht aus der Datei.** `ZPRZ1`/`ZBD1P` stehen wie in SAP als `2,000`. Auf Wert-Ebene ist das nach D-035 mehrdeutig (ein Trenner, drei folgende Ziffern), und T052 enthält keinen eindeutigen Wert, aus dem sich die Notation der Datei ableiten ließe. Das ist kein Fall für die Notationserkennung: `ZPRZ*` und `ZBD1P` sind Prozentsätze mit festem SAP-Format, keine Beträge – Sprint 3 typisiert sie beim Staging über das Feld. Der Test `test_amounts_parse_as_decimal_with_two_places` prüft deshalb nur die Betragsspalten (`WRBTR`, `DMBTR`, `SKFBT`, `SKNTO`). Verworfen: Prozentsätze mit zwei Nachkommastellen schreiben (wäre kein SE16N-Format mehr).
- **D-049 · 2026-08-30 · Die Hygiene-Regeln sind über das Postenfenster abgegrenzt.** AR-HYG-001/AP-HYG-001: kein Posten im gesamten Postenfenster **und** `ERDAT` vor Fensterbeginn. AR-HYG-002: `ERDAT` im Fenster, älter als 12 Monate, kein Posten. Grund: "kein Umsatz seit 30 Monaten" war in einem 24-Monats-Fenster nicht darstellbar – 30 Monate vor dem Datenstand liegen vor dem Fensterbeginn, die Aussage wäre also identisch mit "gar keine Posten" und damit nicht von AR-HYG-002 zu unterscheiden gewesen. `CATALOG.md` und `SPRINT-2.md` sind entsprechend korrigiert; die 40 bzw. 25 Löschkandidaten bekommen in Aufgabe 2 ein `ERDAT` vor Fensterbeginn. Entschieden von Victor.
- **D-050 · 2026-08-30 · "~65 % ausgeglichen" zählt Rechnungen, und die Offen-Wahrscheinlichkeit hängt am Alter.** Junge Rechnungen sind meist offen, alte meist bezahlt (85/55/35/28 % nach 30/60/180/mehr Tagen); über alle Rechnungen ergibt das rund zwei Drittel ausgeglichen. Grund: eine feste Quote über alle Belege hätte zwei Jahre alte offene Rechnungen in gleicher Dichte erzeugt wie taufrische – für einen Demo-Mandanten, den Finanzleute ansehen, wäre das der erste Fehler, der auffällt. Weiteres Postenmodell: ein Beleg = eine Personenkontenzeile (`BUZEI 001`, keine Sachkontenzeilen), eine Zahlung gleicht genau eine Rechnung aus, Gutschriften bleiben offen. Verworfen: 35 % offene **Posten** (hieße 48 % bezahlte Rechnungen und damit ein unrealistisch schlechtes Zahlungsverhalten).
- **D-051 · 2026-08-30 · `volume_12m` ist das Rechnungsvolumen brutto nach Buchungsdatum, unabhängig vom Ausgleich; F-004 und F-005 werden korrigiert.** Definition: Summe der Rechnungsbeträge (Belegarten Rechnung) mit Buchungsdatum in den 12 Monaten vor `data_as_of`, abzüglich Gutschriften im selben Fenster. Der Begriff war nirgends definiert, und beim Bau der Ankerfälle fielen zwei Beispiel-Findings auf, die nur unter der Lesart "nur ausgeglichene Rechnungen" aufgingen: F-004 nannte 0,00 bei drei offenen Posten ab 03.11.2025, F-005 nannte 240.620,00 – exakt die Skontobasis der 23 bezahlten Rechnungen, obwohl acht weitere Rechnungen offen sind. Victor hat die Frage entschieden (Regel 1: nicht stillschweigend anpassen, sondern fragen): das Relevanzgewicht soll zeigen, wie viel Geschäft über den Partner läuft, nicht wie viel davon schon bezahlt ist – sonst sänke die Relevanz eines Partners genau dann, wenn seine Forderungen alt werden. Folge: `F-004.relevance.volume_12m` 0,00 → 8.930,00, `F-005.relevance.volume_12m` 240.620,00 → 259.020,00 (240.620,00 bezahlt + 18.400,00 offen). Definition ergänzt in `docs/GLOSSARY.md`, `docs/CONCEPT.md` (Block 2) und als Kommentar an `bp_relevance.volume_12m_local`; Ankertabelle in `docs/specs/SPRINT-2.md` nachgezogen. Verworfen: Lesart "nur ausgeglichene Rechnungen" (hätte beide Findings unverändert gelassen, macht die Relevanz aber vom Zahlungsverhalten abhängig) und "Summe der Zahlungsbeträge" (zöge gezogenes Skonto vom Volumen ab).
- **D-052 · 2026-08-30 · Cluster- und Paar-Regeln liefern ein Finding je Cluster, nicht je Konto.** Betrifft AR-DUP-001, AP-DUP-001, CROSS-DUP-001 und AP-CON-001: `entity.bp_key` ist das führende Konto, die übrigen stehen in `entity.related_bp_keys`. So ist F-002 gebaut (C:0000100234 mit related C:0000100987), und so entsteht genau eine Arbeitsaufgabe je Sachverhalt statt zweier, die sich widersprechen können. Bei CROSS-DUP-001 sitzt das Finding fest auf dem Debitor (`side: CROSS`, `role: CUSTOMER`), damit die erwartete Liste eindeutig ist. Entschieden von Victor. Verworfen: ein Finding je beteiligtem Konto (doppelte Zahl, widerspricht F-002).
- **D-053 · 2026-08-30 · AR-VAL-002/AP-VAL-002 normalisieren den Wert vor der Formatprüfung und prüfen gegen das Präfix im Wert, nicht gegen das Sitzland.** Anlass ist der Ankerfall C:0000100234 mit `STCEG = "AT U12345678"` bei Sitzland DE: ohne Normalisierung wäre das zugleich ein Präfix- und ein Formatfehler, und alle 25 Präfix-Defekte lieferten ein zweites Finding. Nach der Normalisierung (Leer- und Trennzeichen entfernt) ist `ATU12345678` ein gültiges AT-Format, der Anker liefert genau ein Finding (AR-VAL-001), und die Mengen "falsches Präfix" und "falsches Format" bleiben getrennt. Fachlich ist das auch die richtige Aussage: welches Land gemeint war, sagt das Präfix, nicht der Sitz. Entschieden von Victor.
- **D-054 · 2026-08-30 · Erwartete Findings können ein `from_rule_version` tragen und gelten bis dahin als bekannt-offen.** Die beiden Doppelzahlungspaare über Kreditoren-Dubletten hinweg findet AP-LEA-001 in Version 1.0 nicht (die Regel vergleicht Belege eines Kontos). Sie stehen trotzdem mit `expected`-Eintrag und `from_rule_version: "1.1"` in der Liste; der Regressionstest weist sie aus, solange die Regel darunter liegt, und macht sie ab 1.1 zur Pflicht. Grund: die erwartete Liste bleibt fachlich wahr und muss beim Versionssprung nicht angefasst werden (Regel 1). Entschieden von Victor. Verworfen: `expected: []` mit Notiz (die Erwartung wäre beim Sprung auf 1.1 von Hand nachzutragen).
- **D-055 · 2026-08-30 · Buchungskreisabhängige Regeln liefern ein Finding je (Konto, Buchungskreis).** Betrifft AR-COM-002, AP-COM-003 und AR-CON-002: ein Konto in zwei Buchungskreisen erzeugt zwei Findings, `entity.company_code` ist gesetzt. So steht es in der Spec-Zeile zu AR-CON-002 ("einer in beiden Buchungskreisen → zwei Findings"), und die SAP-Handlungsanweisung (XD02/XK02 je Buchungskreis) bleibt eindeutig. Entschieden von Victor.
- **D-056 · 2026-08-30 · Die Löschkandidaten entstehen aus bestehenden Konten, indem der Defekt ihre Posten entfernt.** AR-HYG-001/AP-HYG-001 verlangen `ERDAT` vor Fensterbeginn und keinen Posten im Fenster; im Basis-Mandanten hat jeder Partner Posten. Die 40 Debitoren und 25 Kreditoren werden deshalb aus den bestehenden Konten gezogen (bevorzugt kleine, ohne offene Posten) und ihre Posten gelöscht. Damit bleiben die Kontenzahlen 2.000/1.500 aus `SPRINT-2.md` und `testdata/README.md` gültig; die Basis-Invariante "jeder Partner hat Posten" gilt weiter für den defektfreien Mandanten und wird für den ausgelieferten um die Löschkandidaten eingeschränkt. Entschieden von Victor. Verworfen: zusätzliche Konten ohne Posten anlegen (hätte Kontenzahlen und Manifest gegen die Spec verschoben).
- **D-057 · 2026-08-30 · `decision.assigned_to` als optionales Feld im Finding-Schema.** Eine getroffene Entscheidung hat oft einen Bearbeiter, der die Umsetzung übernimmt; ohne Feld landet der Name im Freitext von `reason` und ist nicht auswertbar. Optional, weil eine Entscheidung auch ohne Zuweisung vollständig ist (`by`, `at`, `reason` bleiben die Pflichtfelder). Zugleich in F-006 der Vorsatz "Kein Soll ermittelbar – keine zweite Quelle." aus `proposed.display` gestrichen: Stufe C ohne `proposed.value` zeigt die Karte selbst an, der Satz war Doppelung. Entschieden von Victor.
- **D-058 · 2026-08-30 · AR-VAL-001 greift nur bei einem Buchstabenpräfix.** Die Regel verglich bisher die ersten zwei Zeichen der normalisierten USt-IdNr. mit dem Sitzland und verlangte nur `length >= 4`. Der Defekttyp „Steuernummer im USt-IdNr.-Feld" (`123/456/78901`, der häufigste Formatfehler in der Praxis) hätte damit zwei Findings erzeugt: AR-VAL-002 wegen des Formats **und** AR-VAL-001, weil `12` nicht `DE` ist – mit dem sinnlosen Soll-Vorschlag `DE345678901`. Ein Wert ohne Buchstabenpräfix behauptet kein Land, das dem Sitzland widersprechen könnte; das ist ein Format-, kein Präfixfehler. SQL und Klartext der Regel sind entsprechend ergänzt (Regel 10). Entschieden von Victor. Verworfen: beide Findings zulassen.
- **D-059 · 2026-08-30 · `defects.yaml` ist an ihren Seed gebunden und nennt jedes Zielkonto beim Namen.** Die Datei trägt `seed: 20260830`; ein Lauf mit anderem Seed bricht mit klarer Meldung ab und verweist auf `mdq demo generate --no-defects`. Grund: die Defekte nennen konkrete Kontonummern, damit Victor in der Datei sieht, welches Konto welchen Fehler trägt, und einen Defekt ohne Codeänderung ergänzen kann. Kriterienbasierte Auswahl zur Laufzeit („nimm 40 kleine Konten ohne OP") wäre seedunabhängig, aber die Datei sagte dann nicht mehr, worauf sie wirkt, und jede Basis-Änderung verschöbe stillschweigend die erwarteten Findings. Verworfen: Auswahl über Kriterien im Generator, stilles Überspringen unbekannter Konten.
- **D-060 · 2026-08-30 · Die Defekt-Schicht arbeitet auf dem Modell, nicht auf den Exportzeilen.** `apply_defects` verändert `Partner`- und `FiItem`-Objekte über `dataclasses.replace`; die Zeilen entstehen danach unverändert wie zuvor. Grund: Beträge bleiben `Decimal` und Schlüssel typisiert (Regel 2), und ein Defekt, der Posten neu aufbaut (Ankerfälle, Skontoverlust), rechnet mit Geld statt mit deutschen Zahlentexten. Dafür haben `Partner` und `CompanyCodeData` neue Felder bekommen (`name2`, `deletion_flag`, `posting_block`, `payer_bp`, `invoice_check`), die der Basis-Mandant auf den unauffälligen Werten lässt – der erzeugte Basis-Mandant ist byte-identisch geblieben. Verworfen: Defekte auf den fertigen Zeilen anwenden (Beträge als Text, keine Prüfung möglich).
- **D-061 · 2026-08-30 · Belegungsplan: ein Konto trägt höchstens einen Defekt, gewollte Überschneidungen werden angemeldet.** `defects.yaml` kennt dafür das Feld `overlaps`. Grund: sonst entstünde ein Finding, das kein einzelner Defekt erklärt, und die Regression könnte einen Treffer nicht mehr eindeutig zuordnen (D-045). Angemeldet sind genau zwei Fälle: die fünf Debitoren mit falschem USt-ID-Präfix, deren Dublette das Soll liefert (F-001/F-002), und die beiden Doppelzahlungen über zwei Kreditorenkonten. Zusätzlich prüft der Generator jeden Defekt nach dem Anwenden gegen seine Zusage: `expected: []` und ein gemeldeter Treffer schliessen sich aus.
- **D-062 · 2026-08-30 · `last_activity_on` ist das späteste Datum aus Buchungs- und Ausgleichsdatum aller Posten des BP.** Auch dieser Begriff war nirgends definiert, und die Beispiel-Findings ließen zwei Lesarten zu: die Ankertabelle in `SPRINT-2.md` schreibt für C:0000100234 „27 Zahlungen, letzte 12.08.2026" (Zahldatum), F-004 nennt für C:0000101502 dagegen 03.11.2025 – ein Konto ganz ohne Zahlung. Victor hat entschieden: gezählt wird jede Bewegung, und ein spät ausgeglichener alter Beleg gilt als Aktivität am Ausgleichstag. Ein Konto ohne Posten hat `null`. Folgen im Generator: die offenen Rechnungen eines Ankers werden nicht nach dessen letzter Zahlung datiert, und die drei offenen Posten von C:0000101502 liegen alle am 03.11.2025 (die Spec fixiert nur „ältester 03.11.2025"). Definition ergänzt in `docs/GLOSSARY.md`, `docs/CONCEPT.md` und als Kommentar an `bp_relevance.last_activity_on`. `engine/tests/test_demo_defects.py` prüft alle drei Relevanzgrößen aller sechs Beispiel-Findings gegen die erzeugten Daten. Verworfen: nur Zahlungsbelege zählen (F-004 wäre `null` und müsste geändert werden), nur Buchungsdaten zählen.
- **D-063 · 2026-08-30 · Engine und UI werden als finaler Produktcode gebaut; Lux steigt später in die bestehende Codebasis ein.** Löst D-006 ab. `engine/` und `ui/` sind kein Prototyp und werden nicht neu gebaut – sie bekommen dieselbe Sorgfalt wie `logic/`: Tests, Klartext, Determinismus, keine stillen Abkürzungen. Betrieb, Lizenz, Updates und Auth sind nicht gestrichen, sondern spätere Sprints. Grund: ein Wegwerf-Prototyp lädt zu Abkürzungen ein, die beim Einstieg eines zweiten Entwicklers teuer werden; die Codebasis, in die Lux einsteigt, ist genau diese. Folge: „Prototyp" und „austauschbar" sind in `CLAUDE.md`, `engine/README.md`, `ui/README.md`, `docs/CONCEPT.md`, `docs/specs/SPRINT-1.md` und den Modul-Docstrings gestrichen; der Wortlaut von D-001 und D-011 ist entsprechend eingeordnet. Verworfen: Prototyp beibehalten und später neu bauen (doppelte Arbeit, und der fachliche Kern steckt inzwischen in Engine-Code wie Loader, Executor und Demo-Generator), sowie Betrieb/Auth jetzt schon einzubauen (kein Kunde, kein Betriebsmodell – siehe D-003).
- **D-064 · 2026-08-30 · MDQ ist ein eigenständiges Produkt; die Integration mit der Cash App läuft über Export-Artefakte, nicht über gemeinsamen Code.** Ausgetauscht werden Dateien: Zahler-Mapping, bereinigte IBAN-Liste und Zahlverhaltensprofile als JSON/CSV. Kein gemeinsames Paket, keine geteilte Datenbank, kein Aufruf in beide Richtungen. Grund: MDQ muss ohne Cash App verkaufbar und lauffähig bleiben, und der Türöffner-Nutzen (CONCEPT.md, Abschnitt 1) entsteht schon aus den Artefakten. Geteilter Code würde beide Produkte aneinander binden – jede Schemaänderung in MDQ zöge ein Release der Cash App nach sich. Verworfen: gemeinsame Bibliothek für Stammdaten-Normalisierung (Kopplung), direkter Datenbankzugriff der Cash App auf MDQ-Läufe (macht ein internes Laufformat zum Vertrag).
- **D-065 · 2026-08-30 · `reference_norm` wird beim Staging großgeschrieben, genau wie im Mapping definiert.** `logic/mappings/sap_ecc.yaml` bildet `reference_norm` als `upper(regexp_replace(XBLNR, '[^A-Za-z0-9]', '', 'g'))` ab, das kanonische Schema kommentiert das Feld mit „nur [A-Z0-9]". Das ist die verbindliche Definition; Sprint 3 setzt sie beim Staging so um, und keine Regel normalisiert nachträglich selbst. Grund: AP-LEA-001 vergleicht Referenzen auf Gleichheit und mit Levenshtein ≤ 1; der Defekt-Katalog enthält ausdrücklich Groß-/Kleinschreibung als Referenzvariante (`RE-4711` / `re4711`). Ohne Großschreibung im Staging fände die Regel diese Paare nicht oder nur über einen Zufallstreffer der Levenshtein-Schwelle. Verworfen: Normalisierung in jeder Regel wiederholen (dieselbe Logik an vielen Stellen, Regel 10 wäre je Regel neu zu prüfen), Vergleich case-insensitiv im SQL (verschiebt die Definition aus dem Mapping in den Regeltext).
- **D-066 · 2026-08-30 · Testfälle im Regelkopf nennen nur Konten aus `defects.yaml`; der Katalog trägt die Datenlage in einer eigenen Spalte `Testdaten`.** Drei Teile. (a) `tests.hits/no_hits/edge` dürfen nur bp_keys enthalten, die `testdata/demo_mandant/defects.yaml` namentlich nennt. Ein beliebiges Konto aus dem Basis-Mandanten wäre zwar ein gültiger `no_hit`, würde aber bei jeder Generator-Änderung stillschweigend auf ein anderes Konto zeigen – der Testfall bliebe grün und meinte etwas anderes. `engine/tests/test_catalog.py` prüft das, zusätzlich stehen alle `hits` in `expected_findings.yaml` und keiner der `no_hits`. `edge` bleibt unbeschränkt: ein Grenzfall ist absichtlich mal Treffer (C:0000100028, zwei Buchungskreise), mal Nicht-Treffer (C:0000100125, Steuernummer im STCEG). (b) Damit ist auch die zweite Hälfte von D-021 fällig: leere `hits`/`no_hits` sind ab jetzt ein Fehler im Loader, nicht mehr ein Hinweis; `Rule.warnings` und die Hinweiszeile in `mdq rules list` entfallen ersatzlos. `edge` darf leer bleiben – nicht jede Regel hat einen Grenzfall, und ein erfundener wäre schlechter als keiner. (c) `logic/rules/CATALOG.md` bekommt die Spalte `Testdaten` mit `Defekt` (19 Regeln) oder `ohne Testfall` (17 Regeln), geprüft gegen `defects.yaml`. Die Spalte steht neben, nicht in `Status`: der Status beschreibt den Reifegrad der Regel, der Vermerk die Datengrundlage – eine Regel kann `spec` sein und trotzdem keinen Defekt haben. Gelesen wird der Katalog von `engine/mdq/catalog.py`, das nur ID, Status und Testdaten streng prüft; Titel, Schwere und Stufe bleiben Rohtext, weil die Stufen-Spalte bewusst Fließtext enthält („B (Soll = meistgenutzte ZTERM aus Belegen)"). Der Abgleich dieser Felder mit dem Regelkopf braucht erst eine Konvention und gehört nach Sprint 3. Verworfen: Vermerk an die Status-Spalte anhängen (`draft · ohne Testfall`, vermischt zwei Aussagen), Spalte automatisch aus `defects.yaml` generieren (der Katalog ist Victors Arbeitsstand und wird von Hand gelesen und gepflegt), Regelloader gegen `defects.yaml` prüfen lassen (Produktcode dürfte dann `testdata/` nicht mehr entbehren).
- **D-067 · 2026-08-30 · KI-Schicht: Extraktion, nicht Entscheidung.** Ein Modell darf Text zerlegen und normalisieren; es darf nicht entscheiden, ob ein Finding entsteht, und keinen Euro-Betrag liefern. Acht Festlegungen:
  1. **Ort:** Die Schicht sitzt im Staging, vor dem kanonischen Modell. Die Findings-Regeln bleiben deterministisches SQL auf dem kanonischen Schema (D-002), und Euro-Beträge kommen nie aus einem Modell – sie werden aus Belegwerten gerechnet, mit offengelegter Rechnung.
  2. **Ablauf:** Wörterbuch → Regex → Modell, in dieser Reihenfolge; das Modell läuft nur, wenn die ersten beiden scheitern. Ergebnisse werden je Eingabe-Hash gecacht, Temperatur 0, der Prompt ist versioniert, die Ausgabe strukturiert über ein JSON-Schema. Damit bleibt derselbe Input bei demselben Ergebnis (Regel 9).
  3. **Herkunft:** Jedes normalisierte Feld trägt `source` (`dictionary` | `regex` | `model`) und eine Konfidenz. Ein Finding, das auf einem Modell-Feld beruht, zeigt das in der Quellenlage und kommt ohne zweite Quelle nicht über Stufe B. Schadensklasse 1 bleibt davon unberührt – nie Stufe A (Regel 11, D-005).
  4. **Provider-neutral:** OpenAI-kompatible Schnittstelle, angebunden über Konfiguration statt Code. Pilot-Standard ist Azure OpenAI im Tenant des Kunden (EU-Region, kein Training auf den Daten, AVV). Lokal (vLLM/Ollama) ist eine Option mit GPU oder als Nachtlauf; Felder aus einem lokalen Modell tragen dann nur Stufe C. Jeder Modellaufruf ist ein externer Aufruf und braucht denselben ausdrücklichen Schalter wie VIES – in Tests immer aus (Regel 7).
  5. **Zwei Modellklassen:** ein kleines Modell für die Extraktion (lokal lauffähig) und ein großes für Weltwissen im Dubletten-Graubereich (Cloud, optional, abschaltbar). Das große Modell liefert Hinweise und Begründungen, keine Findings.
  6. **Regression:** eine eigene Testdatei mit hässlichen Adressen, Namen und Zahlungstexten samt erwarteter Zerlegung. Jede Prompt-Änderung verlangt einen Testlauf; die erwartete Zerlegung wird nie an die Modellausgabe angepasst (Regel 1).
  7. **Mapping-Assistent:** unbekannte Spalten beim Import bekommen einen Modellvorschlag – Zielfeld oder „ignorieren" – mit Begründung. Ein Mensch bestätigt; ohne Bestätigung bleibt die Spalte ein Fehler mit Spaltenname (Regel 4).
  8. **Lernschleife:** Ablehnungen mit Grund und Graubereich-Fälle werden so protokolliert, dass sie als Beispiele für Prompts und spätere Regeln taugen. Erfassung in der UI ab Sprint 5, Auswertung ab Sprint 4b.

  Grund: die Stärke eines Modells ist das Lesen unsauberer Texte, seine Schwäche die Reproduzierbarkeit. Beides trennt diese Schicht sauber – oben deterministische Regeln mit nachvollziehbaren Euro-Beträgen, unten eine Zerlegung, deren Herkunft am Feld steht. Umsetzung als **Sprint 4b** nach Sprint 4; Verweis in `docs/CONCEPT.md` Abschnitt 7, Platzhalter in `docs/specs/README.md`. Verworfen: Modell als Tie-Breaker über Findings (macht das Ergebnis unreproduzierbar und die Präzisionsziele aus D-004 unprüfbar), Modell erst nach dem kanonischen Modell einsetzen (die Normalisierung ist genau die Stelle, an der Text scheitert), ein fester Anbieter im Code (jeder Kunde bringt eigene Auflagen mit).
- **D-068 · 2026-08-30 · Der Regressionsvergleich läuft über den Schlüssel `(rule_id, bp_key, company_code, finding_key)` und sortiert Abweichungen in sechs Töpfe.** Der Vergleichsschlüssel kann nicht die `finding_id` sein: die ist ein Hash über `bp_key, company_code, source_table, source_field, current_value, finding_key` und aus einer Zeile in `expected_findings.yaml` nicht rekonstruierbar. `document_no` gehört bewusst nicht in den Schlüssel – es ist ein Hinweis für den Menschen; was zwei Findings derselben Regel und desselben BP unterscheidet, entscheidet die Regel über ihre `finding_key`-Spalte. Zwei erwartete Zeilen mit demselben Schlüssel sind deshalb ein Fehler beim Laden, mit derselben Meldung wie beim Executor („der Regel fehlt eine finding_key-Spalte"). Den `finding_key` liefert die neue Funktion `execute_rule_rows` neben dem Finding; er aus `entity.documents` zurückzurechnen wäre nur für AP-LEA-001 richtig. `execute_rule` bleibt unverändert und ruft sie auf. Sechs Töpfe: **fehlend**, **unerwartet** und **abweichend** lassen den Test scheitern, **bekannt offen**, **vorzeitig erfüllt** und **Regel fehlt** sind Hinweise. `abweichend` (gleiche `rule_id` und `bp_key`, anderer Buchungskreis oder `finding_key`) ist ein eigener Topf, weil ein falscher Buchungskreis sonst als ein Fehlend **plus** ein Unerwartet erschiene und die Ursache verschwände. `Regel fehlt` ist ein Hinweis, weil sonst 198 der 230 erwarteten Findings den Test bis zur letzten der 19 Regeln rot hielten – ein dauerhaft roter Test sichert nichts ab. `vorzeitig erfüllt` (D-054-Finding, das schon unter seiner `from_rule_version` auftaucht) ist ebenfalls nur ein Hinweis und nennt die Konsequenz: `from_rule_version` in `defects.yaml` prüfen und absenken. Versionen werden als Zahlen-Tupel verglichen, nicht als Text (sonst wäre `1.10` kleiner als `1.9`). Der Lauf selbst braucht das Mapping SAP → kanonisch und ist bis Sprint 3 mit `pytest.mark.skip` markiert, nicht gelöscht; ein zweiter Test hält fest, dass `mdq run` heute mit Exit 2 abbricht – baut Sprint 3 den Lauf, wird dieser Stolperdraht rot und zwingt dazu, den `skip` anzufassen. Das Modul liegt in `mdq/`, nicht in den Tests: derselbe Vergleich ist beim Kunden nützlich, sobald ein Lauf gegen einen freigegebenen Stand geprüft wird; die Pfade kommen von außen. Entschieden von Victor. Verworfen: drei Töpfe wie in der Spec (Fehlend/Unerwartet/Abweichend genügen nicht, solange 16 Regeln fehlen), `finding_id` als Schlüssel (nicht rekonstruierbar), `execute_rule` in der Signatur ändern (bräche 44 Executor-Tests ohne Gewinn), Regressionsvergleich als reiner Testcode (beim Kunden nicht nutzbar).
- **D-069 · 2026-08-30 · Schema-Erweiterungen aus dem UI: vier optionale Felder, Schema-Version als Metaschlüssel, Feldnamen nach Spec.** Der UI-Strom hat vier Lücken gemeldet, in denen die Karte Werte aus Fließtext zurückrechnen müsste (`../mdq-ui/ui/NOTES.md`); alle vier sind jetzt Felder. `evidence[].reference_kind` (Enum aus acht Werten) sagt, **was** die Referenz ist – `source_type` unterscheidet Beleg, Stammfeld und Cluster nicht, sie stehen alle unter `deterministic` bzw. `model`. `entity.documents[]` trägt zusätzlich `reference`, `document_date`, `cleared_on`, `amount`, `currency`; damit muss der Betrag je Beleg nicht länger aus `impact_eur` geraten werden. `entity.records[]` (`{bp_key, fields}`) macht den Feld-für-Feld-Vergleich bei Dubletten möglich, `proposed.golden_record` nennt je Feld den besten Wert mit Herkunft (`value`, `source_bp_key`, `source_type`) und löst damit das Glossarversprechen ein. Sieben Festlegungen dazu:
  1. **Version:** Das Schema trägt den Metaschlüssel `"version": "1.1"`, kein Finding trägt eine Schema-Version. Die Version beschreibt den Vertrag, nicht den Einzelfall; ein Pflichtfeld je Finding hätte alle sechs Beispiele, `conftest.py` und den UI-Vertrag auf einen Schlag ungültig gemacht. `findings.schema_version()` liest ihn, `mdq run` schreibt ihn nach `run.json` (Sprint 3, Aufgabe 4). Verworfen: Version im `$id` (zwei Wahrheiten, sobald jemand nur eine anfasst), Pflichtfeld je Finding.
  2. **Alles optional.** Kein neues Feld ist Pflicht, keine bestehende Regel muss etwas liefern. Eine Regel füllt, was sie hat; was sie nicht hat, bleibt weg statt `null`-Rauschen zu erzeugen.
  3. **`iban_masked` ist per Pattern maskiert:** höchstens die ersten vier und die letzten vier Zeichen sichtbar, dazwischen `…`, `...` oder `***` (`DE44 …49 32`). Eine vollständige IBAN wird vom Schema abgelehnt. Grund: Schadensklasse 1 lebt von Bankdaten, und ein Feld, das eine ganze IBAN aufnehmen kann, nimmt früher oder später eine auf (Regel 8). Das Pattern ist die einzige Stelle, die das verhindert, bevor ein Finding geschrieben ist.
  4. **Kein Zwang auf `category: duplicate`.** `records` und `golden_record` sind für Dubletten gedacht, aber nicht darauf eingeschränkt: AP-CON-001 (zwei Kreditoren mit derselben IBAN, Sprint 3, Aufgabe 7) ist `consistency` und braucht denselben Kontovergleich. Eine vierte Schema-Invariante hätte diese Regel blockiert, bevor sie geschrieben ist. Verworfen: `if category != duplicate then not golden_record`.
  5. **Feldnamen folgen der Spec, nicht der UI-Rückmeldung.** `name` statt `name1`, `open_items` + `currency` statt `open_items_eur` – der Währungsname steckt seit D-030 nicht mehr im Feldnamen, und die Währung steht neben dem Betrag (Regel 2). Der UI-Strom zieht `ui/src/types/finding.ts` nach; dort steht `relevance` ohnehin noch auf den alten Namen.
  6. **`golden_record` listet seine elf Felder namentlich** statt sie über `propertyNames` gegen eine Liste zu prüfen. Beides lehnt einen Tippfehler ab, aber nur die benannte Fassung meldet ihn **mit Feldnamen** (`Additional properties are not allowed ('ust_id' was unexpected)`); `propertyNames` meldet nur „verletzt enum" am Elternknoten und lässt den Leser suchen – das widerspricht Regel 4. Dass die beiden Feldlisten (`records[].fields` und `golden_record`) nicht auseinanderlaufen, sichert ein Test statt einer gemeinsamen Definition.
  7. **`$defs` für Wiederholungen:** `amount_string`, `currency_code`, `date_or_null`, `bp_key`, `iban_masked`, `evidence_source_type`, `golden_record_entry`. Das Betragsmuster stand vorher viermal wörtlich im Schema; die Fehlermeldungen bleiben unverändert, weil `$ref` den Fehler des Zielschemas meldet (`pattern`, `enum`) und nie den beanstandeten Wert zitiert (D-015).

  **Beispiele:** F-003 bekommt beide Belege vollständig (32.000,00 EUR je Beleg, Referenz, Beleg- und Ausgleichsdatum – alles stand schon wörtlich in den Evidenztexten) und den Netting-Nachweis als `reference_kind: netting`. F-002 bekommt beide Kontosätze; gefüllt wird ein Feld nur, wenn es für **beide** Konten belegt ist (Name, Straße, PLZ, Ort, USt-ID, offene Posten, Währung). Land und letzte Aktivität stehen nur für das führende Konto in der Datei und bleiben deshalb weg: einseitig gefüllt würde die Vergleichstabelle eine Abweichung anzeigen, wo nur eine Angabe fehlt. Die USt-IDs beider Konten stammen aus dem ausdrücklich verknüpften F-001 (`related_finding_ids`, dasselbe Cluster) – nichts ist erfunden. `golden_record` trägt in F-002 strikt nur `vat_id`: die Survivorship-Evidenz belegt das führende Konto, nicht die beste Schreibweise je Feld; je Feld füllt das erst die Dublettenregel in Sprint 4 (entschieden von Victor). `reference_kind` steht in acht von dreizehn Evidenzen – nur dort, wo die Referenz eindeutig ist. Fünf bleiben leer: ein bloßer BP-Schlüssel (`C:0000100987`), eine Tabelle ohne Feld (`KNVP`, `TIBAN`), ein Änderungsbeleg (`CDHDR …`, kein FI-Beleg) und ein Customizing-Eintrag (`T052 ZB02`) passen in keinen der acht Werte. Die UI zeigt die Referenz dann wörtlich, wie von ihr vorgesehen. `statement` ist dabei als **Postenübersicht über ein Konto und einen Zeitraum** gelesen (`BSID 1000`, `BSAD 2024-09..2026-08`) und nicht als Kontoauszug – ein Bankauszug steht schon als `source_type: bank_statement`.

  **Kein Engine-Code:** `documents` und `evidence` kommen als JSON-Spalten aus der Regel und werden vom Executor nur durchgereicht – die neuen Unterfelder fließen ohne Änderung mit und werden vom Schema geprüft. `records` und `golden_record` bekommen ihre Executor-Anbindung mit der ersten Dublettenregel (Sprint 4); sie jetzt zu verdrahten hieße, einen Vertrag ohne Aufrufer zu schreiben.

  Punkt 5 der Aufgabe (Konsistenz von `title` und `decision.assigned_to`) ist geprüft: `title` ist Pflicht mit `minLength: 1`/`maxLength: 120` und in allen sechs Beispielen gesetzt, `decision.assigned_to` steht optional im Schema. Kein Beispiel übt es aus – alle sechs stehen auf `status: open` mit `decision: null`, und ein erfundener Entscheidungssatz wäre ein Testartefakt in Victors fachlicher Spec. Abgesichert ist das Feld stattdessen durch einen Test.
- **D-070 · 2026-08-30 · numpy als Abhaengigkeit fuer DuckDB-UDFs. Ueberholt durch D-071 – der Eintrag bleibt als Spur der Messung stehen.** Die Typisierung sollte `formats.py` als DuckDB-UDF (`con.create_function`) aufrufen, damit es die Betrags- und Datumslogik genau einmal gibt. DuckDBs Python-UDFs setzen in dieser Fassung `numpy` voraus (`'numpy' is required for this operation`); numpy war weder direkt noch mittelbar installiert, und die Stack-Liste in `CLAUDE.md` erweitert man nur mit Eintrag hier. Aufgenommen wurde es deshalb ausdruecklich als **Laufzeitvoraussetzung der Datenbank, nicht als Rechenwerkzeug**: MDQ rechnet mit `Decimal`, nie mit numpy-Floats (Regel 2). Die von Victor zur Bedingung gemachte Laufzeitmessung hat den Weg dann verworfen (siehe D-071), und numpy ist wieder aus `pyproject.toml` entfernt. Der Eintrag bleibt, weil die naechste Person, die auf `create_function` stoesst, sonst denselben Versuch noch einmal macht.

- **D-071 · 2026-08-30 · Die Typisierung raw -> staged rechnet in SQL-Makros, nicht in Python-UDFs.** `logic/schema/staging.sql` enthaelt die Makros `mdq_parse_amount`, `mdq_parse_percent`, `mdq_parse_date`, `mdq_parse_integer` und `mdq_parse_flag` samt ihrer Bausteine; `engine/mdq/formats.py` bleibt die Referenz-Implementierung und beschreibt die Faelle im Klartext. Grund ist eine Messung, nicht eine Vorliebe: der zeilenweise Python-UDF kostet rund 75 Mikrosekunden je Wert an der Sprachgrenze – eine UDF, die nichts tut (`lambda x: x`), ist genauso langsam wie eine, die parst. Der Demo-Mandant typisiert rund 680.000 Werte; das waren **49,2 Sekunden**. Mit Makros sind es **1,3 Sekunden** bei gleichem Ergebnis. Victor hatte 20 Sekunden als Grenze gesetzt und den Wechsel fuer diesen Fall vorab entschieden. Fuenf Festlegungen dazu:
  1. **Aequivalenz ist gepruefte Zusage, keine Absicht.** Zwei Implementierungen derselben Logik sind der Preis dieser Entscheidung, und der einzige Weg, ihn tragbar zu halten, ist ein Test, der beide gegeneinander laufen laesst: `test_staging.py` vergleicht Makro und `formats.py` (a) auf einem festen Grenzfallsatz – beide Notationen, das mehrdeutige `1.234`, SAP-Initialwerte, voran- und nachgestellte Vorzeichen, leere Werte, Prozentfelder nach Feldtyp (D-048) – und (b) ueber **jeden eindeutigen Wert jeder getypten Spalte** des Demo-Mandanten, Zelle fuer Zelle. Jede Aenderung an einer Seite verlangt dieselbe an der anderen; beide Dateien sagen das im Kopf.
  2. **Ausgewertet wird in Schichten.** DuckDB setzt Makros textuell ein: ein Aufruf wie `mdq_parse_amount(mdq_clean(v))` kopiert denselben Teilbaum vielfach, und das Binden einer einzigen Abfrage lief in den Minutenbereich. `staging.typed_layers` legt deshalb je Schritt – Vorzeichen und bereinigter Wert, Dezimaltrenner, Zahlliteral, geprueftes Ergebnis – eine Unterabfrage darueber; jedes Makro sieht nur noch Spaltenverweise und laeuft einmal je Zeile.
  3. **Kein stilles Runden.** `mdq_fits` prueft vor der Umwandlung, ob das Literal verlustfrei in die Zielspalte passt. `TRY_CAST('1,234' AS DECIMAL(15,2))` wuerde auf 1,23 runden – ein Betrag mit drei Nachkommastellen ist in einer DECIMAL(15,2)-Spalte aber kein Betrag, sondern ein Hinweis auf eine falsch gelesene Spalte (Regel 4). Dasselbe gilt fuer `ZPRZ1`/`ZBD1P`: `100,000` passt nicht in DECIMAL(5,3), und die SAP-Domaene PRZ23 laesst hoechstens 99,999 zu – ein Wert darueber heisst kaputter Export und wird Reject mit Grund (entschieden von Victor).
  4. **`SHKZG` wird geprueft.** Weder `S` noch `H` ist ein Reject: `amount_signed_local` entsteht genau hier (D-009), und ein dritter Wert machte sein Vorzeichen zur Vermutung.
  5. **Fehlende gemappte Spalten stoppen die Tabelle nicht, unbekannte schon.** Regel 4 nennt ausdruecklich die unbekannte Spalte als Fehler mit Spaltenname – die bricht ab. Eine fehlende gemappte Spalte ist etwas anderes: ein unvollstaendiger Export. Gestagt wird, was geliefert ist, und die Luecke steht als Hinweis im Run-Report (`has_problems` wird wahr). Ob ein Feld unverzichtbar ist, entscheidet das kanonische Modell in Aufgabe 2. Ohne diese Trennung waere `testdata/encoding_samples/` – bewusst ein Ausschnitt weniger Spalten – nicht mehr ladbar gewesen.

  Verworfen: der zeilenweise Python-UDF (per Messung, siehe oben), der vektorisierte Arrow-UDF (`type='arrow'`) – er waere vermutlich schnell genug, braucht aber `pyarrow` als weitere Abhaengigkeit und blieb hier ungetestet, und Victor hat ihn ausdruecklich verworfen –, sowie die Typisierung in einer Python-Schleife mit `executemany` (widerspricht der Stack-Vorgabe, Transformationen in DuckDB-SQL zu halten; zeilenweises INSERT kostet allein rund 1 ms je Wert).

- **D-072 · 2026-08-30 · Die Abbildung `staged → canonical` ist aus dem Mapping erzeugtes SQL, ein `INSERT … SELECT` je Quelltabelle.** `engine/mdq/canonical.py` liest die Feldlisten aus `logic/mappings/sap_ecc.yaml` und die Spaltenliste aus `logic/schema/canonical.sql`; es gibt keine dritte Liste im Code. Was eine Zeile am Kanonischwerden hindert, hat je einen eigenen Weg und ist nie still (Regel 4): leeres Pflichtfeld → `reject` mit kanonischem Feldnamen und Quellspalte; Zeile ohne Stammsatz → `reject` mit dem `bp_key` im Grund; doppelter Primärschlüssel → `reject` für **beide** Zeilen, keine gewinnt stillschweigend; trifft eine Zeile auf einen Schlüssel, den eine andere Quelltabelle schon belegt hat (derselbe Beleg in BSID **und** BSAD), wird die spätere abgelehnt und der Grund nennt beide Quellen. Die Duplikatprüfung ist kein Luxus: ohne sie käme an dieser Stelle eine nackte `ConstraintException` von DuckDB ohne Zeilennummer und ohne Grund. Verworfen: handgeschriebenes SQL je Zieltabelle in `logic/` (zweite Feldliste neben dem Mapping, die auseinanderläuft), Abbildung in Python-Schleifen (Beträge verlören ihren Typ, Regel 2).
- **D-073 · 2026-08-30 · `iban_valid` rechnet schwifty in Python auf den eindeutigen IBAN, nicht als DuckDB-UDF.** Die eindeutigen `iban_norm` (im Demo-Mandanten rund 3.100) werden einmal geprüft und als Tabelle zurückgegeben, die an `bp_bank_account` gejoint wird. Grund ist dieselbe Messung wie bei D-071: ein zeilenweiser Python-UDF kostet rund 75 Mikrosekunden je Wert an der Sprachgrenze. Anders als bei der Typisierung sind es hier aber nicht 680.000 Werte, sondern einige tausend eindeutige – der Umweg über die Sprachgrenze kostet Bruchteile einer Sekunde, und schwifty prüft mehr als die Prüfziffer (Länderlänge und BBAN-Struktur), was in SQL nachzubauen eine zweite Wahrheit wäre. Verworfen: Modulo-97 in reinem SQL (prüft das Länderformat nicht mit), `create_function` (Laufzeit, D-071), Prüfung erst in AR-VAL-003/AP-VAL-003 (die Spalte steht im kanonischen Schema und wäre sonst dauerhaft leer).
- **D-074 · 2026-08-30 · Der Text der Zahlungsbedingung kommt aus T052U mit Sprachschlüssel `D`, nicht `DE`.** `SPRINT-3.md` schreibt „T052U-Text (SPRAS DE bevorzugt)"; der Datenwert in SAP ECC ist der einstellige Sprachschlüssel `D`, und so steht er auch im Demo-Mandanten. Wörtlich genommen hätte die Bedingung keine Zeile getroffen und `payment_terms.description` wäre still leer geblieben. Reihenfolge der Bevorzugung: `D`, dann `E`, dann der alphabetisch erste vorhandene Schlüssel – ein Text ist besser als keiner, und die Wahl ist deterministisch (Regel 9). Verworfen: „DE" wörtlich nehmen, mehrsprachige Texte alle übernehmen (`payment_terms` hat je Bedingung eine Zeile).
- **D-075 · 2026-08-30 · Der Scope-Filter greift vor der referentiellen Prüfung; was er ausschließt, ist kein Reject.** Ein Posten in einem nicht gewählten Buchungskreis ist nicht fehlerhaft, sondern nicht Gegenstand des Laufs. Liefe die Prüfung zuerst, produzierte jeder Lauf mit `--company-codes` Rejects für genau die Konten, die der Anwender bewusst ausgeschlossen hat. Damit der Ausschluss trotzdem sichtbar bleibt (Regel 4), nennt der Report-Block „canonical" den aktiven Scope und je eine Zeile mit der Zahl der dadurch nicht aufgenommenen Partner und Posten; jede Zieltabelle führt „Ausserhalb Scope" als eigene Spalte neben „Rejects". Ein Partner gehört zum Lauf, wenn er mindestens eine `bp_company_code`-Zeile im Scope hat; Nebentabellen ohne eigenen Buchungskreis (KNBK, KNVP) folgen ihrem Partner. Entschieden von Victor. Verworfen: erst prüfen, dann filtern; den Ausschluss gar nicht ausweisen.
- **D-076 · 2026-08-30 · `bp_tax_id.country` bleibt leer, solange KNAS/LFAS nicht gelesen werden.** Das kanonische Schema kommentiert das Feld mit „Land der ID (bei KNAS/LFAS abweichend möglich)"; diese beiden Tabellen stehen nicht im Mapping und werden nicht geliefert. Das Land aus dem Buchstabenpräfix des Wertes abzuleiten wäre genau die Behauptung, die AR-VAL-001 prüfen soll: die Regel vergleicht das Präfix mit dem Sitzland und meldet den Unterschied (D-053, D-058). Ein aus dem Präfix gefülltes `country` hätte diese Prüfung zu einem Vergleich des Wertes mit sich selbst gemacht. Verworfen: Präfix als Land übernehmen, Sitzland des Partners eintragen (falsch, sobald eine ID auf ein anderes Land lautet).
- **D-077 · 2026-08-30 · Pflicht-Quellspalten werden aus `canonical.sql` abgeleitet; fehlt eine, bricht die Stufe hart ab.** Nachtrag zur Staging-Regel „fehlende Spalte = Hinweis" (D-071-Umfeld): Je Zieltabelle gilt jede Zielspalte `NOT NULL` ohne `DEFAULT` als Pflicht, und die laut Mapping zuliefernde Quellspalte damit auch. Fehlt sie in der gestagten Tabelle, endet die Stufe mit Tabellen- und Spaltennamen und der Nennung aller Zielspalten, die an ihr hängen (`DMBTR` trägt `fi_item.amount_local` **und** über SHKZG `amount_signed_local`, D-009); alle Lücken werden gemeinsam gemeldet. Fehlende optionale Spalten bleiben ein Hinweis im Report wie bisher. Grund für den Unterschied zum Staging: dort ist eine fehlende Spalte eine Lücke in einer Zeile, hier ist sie eine Tabelle, die es nicht geben kann – ein Lauf auf der halben Wahrheit ist schlimmer als keiner (Regel 4). Zwei Folgefestlegungen: Nebenziele (`tax_id.*`, TIBAN, T052U, ADR6) haben keine Pflichtspalten, weil sie Zeilen nur erzeugen, wenn der Export das Feld liefert; und der Vorgabewert einer `NOT NULL DEFAULT`-Spalte wird aus dem Schema gelesen statt im Code angenommen – die encoding_samples liefern `SPERR` nicht, und `central_block` muss trotzdem `FALSE` werden. Entschieden von Victor. Verworfen: auch hier nur einen Hinweis ausgeben (`fi_item` ohne Beträge hätte den Lauf lautlos entwertet), eine Pflichtliste von Hand im Code führen (zweite Wahrheit neben dem Schema).
- **D-078 · 2026-08-30 · Mehrere TIBAN-Zeilen zu einem Bankschlüssel: die jüngste `VALID_FROM` gewinnt, Gleichstand ist ein Reject.** Der Join KNBK/LFBK → TIBAN läuft über BANKS/BANKL/BANKN/BKONT, und TIBAN führt Historie. Zwei Zeilen mit demselben `VALID_FROM` und verschiedenen IBAN sind nicht auflösbar; die Bankverbindung wird dann nicht gebildet, sondern mit Grund abgelehnt – eine stillschweigend gewählte IBAN wäre bei Schadensklasse 1 die schlechteste aller Antworten. Sind alle `VALID_FROM` leer, gelten sie als gleich alt. **Offener Punkt:** ein `VALID_FROM` in der Zukunft gewinnt derzeit mit; sobald der Lauf `data_as_of` kennt (Aufgabe 4), wird auf `max(VALID_FROM ≤ data_as_of)` nachgeschärft. Entschieden von Victor. Verworfen: die alphabetisch erste IBAN nehmen, die Bankverbindung ohne IBAN aufnehmen (stumme Lücke).
- **D-079 · 2026-08-30 · `name_norm`, `city_norm` und `street_norm` bleiben in Sprint 3 leer und werden als „Sprint 4" ausgewiesen.** Ihre Normalisierung definiert die Splink-Dubletten-Spec in Sprint 4; sie jetzt ohne diese Anforderungen zu bauen hieße, die Definition zweimal zu schreiben. Geprüft und bestätigt: keine der 14 Regeln aus den Aufgaben 6–8 liest die drei Felder – AR-VAL-005 arbeitet auf dem Rohnamen, CROSS-DUP-001 auf der USt-IdNr., und nur AR-DUP-001/AP-DUP-001 (Sprint 4) nennen „Name+Adresse normalisiert". Damit die leeren Spalten nicht als stille Lücke durchgehen (Regel 4), steht der Verweis an drei Stellen: im Modul-Docstring von `canonical.py`, als Kommentar an `derivations.name_norm` im Mapping und als Hinweiszeile im Run-Report jedes Laufs. Nicht betroffen sind `value_norm` (Steuer-IDs) und `iban_norm`, die aus dem Staging kommen und heute gefüllt werden. Entschieden von Victor. Verworfen: die Normalisierung jetzt bauen (Doppelarbeit, sobald die Dubletten-Spec Anforderungen stellt), die Spalten kommentarlos leer lassen.

- **D-080 · 2026-08-30 · Die Gutschrift des Generators zieht ihre Belegnummer aus dem Nummernkreis ihres eigenen Geschäftsjahrs.** Befund aus der Handprobe zu Sprint 3, Aufgabe 2: `postings.py` vergab die Nummer aus dem Kreis des **Rechnungsjahrs**, während `_credit_memo` das Geschäftsjahr aus dem **Gutschriftsdatum** setzt – und das liegt 5 bis 45 Tage später, also möglicherweise im Folgejahr. Über den Jahreswechsel trafen damit zwei Nummernkreise im selben Geschäftsjahr aufeinander: 37 Paare, 74 Zeilen, ausnahmslos Gutschriften (DG/KG), mit demselben Belegschlüssel `BUKRS|GJAHR|BELNR|BUZEI`. SAP nummeriert nach dem Geschäftsjahr des Belegs; die Nummer kann deshalb erst vergeben werden, wenn der Beleg datiert ist. Umgesetzt ohne Eingriff in die Zufallsströme: `_credit_memo` erzeugt den Beleg wie bisher, die Nummer wird danach per `dataclasses.replace` gesetzt – die Aufrufreihenfolge von `rng` bleibt Zeichen für Zeichen dieselbe (D-047). Wirkung, vollständig geprüft: in 13 der 15 Exportdateien ändert sich **nichts**; in BSID und BSIK ändert sich **ausschließlich** `BELNR`, und zwar bei 674 DG- und 302 KG-Zeilen. Beträge, Konten, Daten, `GJAHR` und alle übrigen Felder sind feldgenau identisch, `expected_findings.yaml` (230 Findings, Verteilung je Regel), `defects.yaml` und `logic/examples/` sind unverändert, ebenso die Kontrollsumme BSID/1000/EUR 123.605.141,17 und alle Zeilenzahlen. Im Manifest ändern sich die beiden sha256 von BSID und BSIK. Ein Stolperdraht in `test_demo_output.py` prüft ab jetzt die Eindeutigkeit des Belegschlüssels über alle vier Postentabellen, damit derselbe Fehler nicht in einer anderen Belegart wiederkehrt. Entschieden von Victor. Verworfen: den Belegschlüssel in `fi_item` um die Quelltabelle erweitern (hätte einen Datenfehler zur Modelleigenschaft gemacht), die Gutschrift auf das Rechnungsjahr zurückdatieren (verschöbe Buchungsdaten und damit Fenster, Relevanz und Findings).
- **D-081 · 2026-08-30 · Der Scope bis zum ersten zahlenden Kunden ist SAP ECC.** Das Mapping, die Extraktionsanleitung und der Demo-Mandant zielen auf ECC (SE16N-Exporte aus KNA1/LFA1/BSID/BSAK und den zugehörigen Tabellen). Ein S/4-Mapping – Geschäftspartner statt getrennter Debitoren- und Kreditorenstämme, ACDOCA statt der BSE*-Tabellen – wird ein eigener Sprint, und zwar erst nach konkretem Kundenbedarf. Bis dahin filtert die Pilotakquise ausdrücklich auf ECC-Landschaften. Grund: beide Welten gleichzeitig zu bedienen verdoppelt Mapping, Testdaten und Regressionsaufwand, bevor auch nur ein Kunde bezahlt hat; und die fachlichen Regeln (Dubletten, Doppelzahlung, Skontoverlust) sind vom ERP-Release unabhängig – sie lesen das kanonische Schema, nicht SAP (Regel 5). Entschieden von Victor. Verworfen: S/4 von Anfang an mitziehen, ein gemeinsames Mapping für beide Releases (die Tabellenstruktur unterscheidet sich zu stark, um sie in einer Datei ehrlich abzubilden).

- **D-082 · 2026-08-30 · AP-LEA-001: die `netted`-CTE erkennt das Netting an der Belegart, nicht nur am Soll-Kennzeichen.** Befund aus derselben Handprobe wie D-080: die Regel schloss ein Rechnungspaar aus, sobald ein Beleg desselben Kreditors mit `debit_credit = 'S'` in gleicher Höhe innerhalb von 180 Tagen existierte. Beim Kreditor ist aber **auch die Zahlung** Soll – und jede bezahlte Rechnung hat definitionsgemäß eine Zahlung in gleicher Höhe. Damit nettete die Regel jedes Paar mit sich selbst weg: auf dem Demo-Mandanten fand sie 0 statt 8 Findings, obwohl sie 11 Paare korrekt gebildet hatte. Das SQL prüft jetzt zusätzlich `doc_type = 'KG'` (Kreditorengutschrift). **Die Version bleibt 1.0 und der Klartext unverändert:** er verlangte von Anfang an eine „Gutschrift/Storno" – nicht die Regel war fachlich falsch, sondern ihre Umsetzung wich vom eigenen Klartext ab (Regel 10). Ein Versionssprung hätte die zwei bekannt offenen Erwartungen mit `from_rule_version: "1.1"` (D-054) fälschlich zur Pflicht gemacht. Die Korrektur steht als Vermerk im Regelkopf, damit sie beim Lesen der Regel auffällt. Zwei Stolperdrähte in `test_executor.py`: „bezahlte Doppelzahlung ohne Gutschrift bleibt ein Treffer" und die Gegenprobe „echte Gutschrift nettet weiterhin" – der alte Stand fiel bei beiden durch. **Offener Punkt:** der Klartext nennt „Gutschrift/**Storno**"; der Demo-Mandant kennt auf der AP-Seite nur KR, KZ und KG, ein echter Kundenexport kann Stornobelegarten (KA und kundeneigene) enthalten. Sobald einer vorliegt, ist die Liste der netting-fähigen Belegarten zu erweitern – heute wäre jede zusätzliche Belegart geraten. Entschieden von Victor. Verworfen: Version auf 1.1 heben (macht die zwei bekannt offenen Erwartungen zur Pflicht, obwohl der Kreditoren-Cluster weiter fehlt), Netting über das Ausgleichsdatum statt die Belegart (die Zahlung gleicht die Rechnung ebenfalls aus).


- **D-083 · 2026-08-30 · Die Hauswährung kommt aus T001; die Tabelle ist Pflicht, nicht ersetzbar.** Bis Sprint 3, Aufgabe 2 stand die Hauswährung nirgends im kanonischen Modell: `fi_item.currency` ist die **Beleg**währung (`WAERS` zu `WRBTR`), `amount_local` (`DMBTR`) steht in der Hauswährung – und die hat in BSID/BSIK kein eigenes Feld. Damit war jeder kanonische Betrag eine Zahl ohne Währung, genau das, was Regel 2 verbietet, und `bp_relevance.currency` (D-030) hätte keine Quelle gehabt. Neu deshalb: `T001` als Quelltabelle (`BUKRS`, `BUTXT`, `WAERS`, `LAND1`) und die kanonische Tabelle `company_code` (`company_code` als Primärschlüssel, `name`, `currency NOT NULL`, `country`). Sie steht als erste in `TARGET_ORDER`, weil sie die Währung trägt, in der die Beträge aller übrigen Tabellen stehen. Da `currency` `NOT NULL` ohne `DEFAULT` ist, macht die bestehende Ableitung aus `canonical.sql` `WAERS` automatisch zur Pflicht-Quellspalte (D-077): fehlt die Spalte, endet die Stufe mit Tabellen- und Spaltennamen. Fehlt die **Datei**, bleibt `company_code` leer und die Relevanzstufe bricht mit dem Tabellennamen ab (Aufgabe 3) – es gibt bewusst keinen CLI-Ersatzwert. Der Demo-Mandant schreibt T001 als 16. Exportdatei (zwei Buchungskreise, beide EUR); der CHF-Mandant für den D-030-Abbruchtest wird damit datengetrieben (Aufgabe 9). Regel-1-Nachweis der Neuerzeugung: die 15 bestehenden Exportdateien sind **byte-identisch**, `testdata/expected/expected_findings.yaml`, `testdata/demo_mandant/defects.yaml` und `logic/examples/` **unverändert**; in `manifest.json` kommt genau ein Eintrag hinzu und `total_rows` steigt um 2 (76.775 → 76.777). Entschieden von Victor. Verworfen: `--house-currency` als CLI-Schalter (verlagert Stammdatenwissen auf den Anwender und ist beim Kunden nicht nachprüfbar), Ableitung aus der Belegwährung der Posten (verwechselt Beleg- mit Hauswährung und ist bei jeder Fremdwährungsrechnung falsch), die Hauswährung als Spalte an `bp_company_code` hängen (sie gehört zum Buchungskreis, nicht zur Beziehung Partner–Buchungskreis, und stünde je Partner erneut da).

- **D-084 · 2026-08-30 · Welche Belegart eine Rechnung ist, steht in einem Wörterbuch – nicht im Code und nicht im Regel-SQL.** `logic/dictionaries/document_types.yaml` ordnet je Seite (AR/AP) jede Belegart (`BLART`) einer Klasse zu: `invoice`, `credit_memo`, `payment`, `reversal`. Zwei Stellen brauchen dieselbe Auskunft, und sie liefen sonst auseinander: die Relevanz (`volume_12m` = Rechnungen minus Gutschriften, D-051) und die Regeln, die auf Gutschriften netten (AP-LEA-001, D-082). Die Engine liest die Datei und **injiziert die Listen den Regeln als `params`**; Regel-SQL liest weiterhin ausschließlich das kanonische Schema (Regel 5) und greift nie auf eine Datei zu. Die Umstellung von AP-LEA-001 auf diesen Weg ist ein eigener Commit, mit unverändertem Klartext und der Probe 8/8 danach. Aufgenommen sind nur Belegarten, die im Demo-Mandanten tatsächlich vorkommen (`DR/DG/DZ`, `KR/KG/KZ`); `reversal` bleibt auf beiden Seiten leer, solange kein Kundenexport eine Stornobelegart gezeigt hat – das ist der offene Punkt aus D-082, und eine erfundene Belegart wäre schlimmer als eine leere Liste. Damit die Lücke nicht stumm wirkt (Regel 4), meldet die Relevanzstufe jede Belegart, die in den Posten vorkommt und keiner Klasse angehört, mit Zahl in den Run-Report – sie zählt sonst unbemerkt nicht ins Volumen. Ein Test hält fest, dass der Demo-Mandant heute keine solche Belegart hat. Verworfen: Listen als Konstante in `relevance.py` (zweite Wahrheit neben AP-LEA-001), die Klassen als DuckDB-Tabelle bereitstellen (Regeln läsen dann eine Tabelle, die nicht im kanonischen Schema steht).
- **D-085 · 2026-08-30 · Die Relevanzbeträge stehen in der Fachlogik ihrer Seite: Forderung und Verbindlichkeit sind beide positiv.** `open_items_local` und `volume_12m_local` entstehen aus `amount_signed_local` (D-009), beim Kreditor mit umgekehrtem Vorzeichen: dort sind Rechnungen Haben und damit negativ signiert. Das ist keine Vorliebe, sondern eine Vorgabe aus den Ankerfällen und den Beispiel-Findings: `SPRINT-2.md` nennt für V:0000200117 „OP 18.400,00", F-005 und F-006 tragen 18.400,00 und 27.300,00 – jeweils positiv, obwohl die zugrunde liegenden Posten negativ signiert sind (Regel 1). Ein Debitor mit Guthabensaldo steht damit korrekt negativ. Verworfen: die rohe Vorzeichensumme (Kreditoren stünden durchgehend negativ und wären mit Debitoren nicht vergleichbar), `abs()` (ein Guthaben sähe aus wie eine Forderung).
- **D-086 · 2026-08-30 · `activity_status`: ein Aktualitätsbegriff, nicht zwei.** Der Wert stand bisher nur als `CHECK (active|dormant|never_posted)` im Schema, ohne fachliche Definition. Jetzt gilt, mit **derselben Zwölfmonatsgrenze wie `volume_12m`**: `never_posted` = kein Posten **und** Anlagedatum (`business_partner.created_on`) innerhalb des Fensters – das Konto konnte sich noch gar nicht bewegen; `dormant` = keine Aktivität im Fenster, ausdrücklich einschließlich der Konten ohne jeden Posten, die vor Fensterbeginn angelegt wurden; `active` = letzte Aktivität im Fenster. Zwei Fenster (eines für das Volumen, eines für die Aktualität) hätten zwei Zahlen erzeugt, die dasselbe behaupten. Ist `created_on` leer, gilt das Konto als vor dem Fenster angelegt und damit als `dormant`: „nie bebucht" ist die stärkere Behauptung und braucht ein Datum. **Verifiziert, nicht angepasst** (Regel 1): die 65 Konten des Demo-Mandanten ohne jeden Posten – 40 Debitoren und 25 Kreditoren aus den `dormant_account`-Defekten – sind ausnahmslos `dormant`, keines `never_posted`; das deckt sich mit der Notiz „angelegt vor Fensterbeginn" in `defects.yaml`. Ein Test prüft genau diese Aufteilung, damit ein künftig im Fenster angelegtes Konto auffällt statt durchzurutschen. Entschieden von Victor. Verworfen: eine eigene Grenze für die Aktualität (18 oder 24 Monate), `never_posted` allein am fehlenden Posten festzumachen (dann wäre jedes ruhende Altkonto „nie bebucht"). **Randnotiz:** `SPRINT-3.md` verweist bei AR-HYG-001 auf eine „Fensterdefinition D-057" – D-057 ist aber `decision.assigned_to`, die Referenz ist falsch. Bezugspunkt ist ab jetzt diese Entscheidung; die Spec wird bei Aufgabe 6 korrigiert.
- **D-087 · 2026-08-30 · Das Relevanzfenster ist links offen und rechts geschlossen.** Gezählt wird `posting_date > data_as_of − 12 Monate` und `posting_date <= data_as_of`. Wäre es beidseitig geschlossen, gehörte der Beleg vom selben Tag ein Jahr zuvor in zwei aufeinanderfolgende Fenster und würde beim Vergleich zweier Datenstände doppelt gezählt. Gibt es den Tag im Zielmonat nicht (29. Februar), gilt der letzte Tag des Monats. Ein Test setzt je einen Beleg exakt auf den Fensterbeginn und auf den Datenstand: der erste zählt nicht mehr, der zweite noch. Verworfen: beidseitig geschlossen, ein Fenster über 365 Tage statt über Kalendermonate (verschöbe sich in Schaltjahren gegenüber der Monatsangabe im Glossar).
- **D-088 · 2026-08-30 · Das Entscheidungsgedächtnis ist ein gepflegtes Artefakt, kein Laufprodukt.** Die getroffenen Entscheidungen stehen in einer YAML-Datei (`--decisions <pfad>`, ersatzweise `<input>/decisions.yaml`) und werden vor der Regelausführung in die kanonische Tabelle `decision_memory` geladen; später schreibt die UI diese Datei. **Nicht** aus dem `findings.json` eines Vorlaufs: das koppelte Läufe aneinander, und der Vergleich zweier Läufe ist ausdrücklich nicht Sprint 3/4. Ein Finding, dessen `finding_id` im Gedächtnis steht, **entsteht weiterhin** und trägt den gespeicherten Status statt `open`, dazu den `decision`-Block mit `by`, `at`, `reason` und `reason_code` – die Whitelist wirkt, aber nichts verschwindet stumm (Regel 4). Drei Prüfungen, weil ein Gedächtnis mit falschen Schlüsseln schlimmer ist als keines: `rule_id` und `bp_key` stehen redundant neben der `finding_id` und müssen zum erzeugten Finding passen – tun sie es nicht, ist der Eintrag nach einer Regeländerung veraltet und der Lauf bricht ab, statt eine Entscheidung auf einen fremden Fall zu übertragen; ein unbekannter `reason_code` ist ein Fehler mit Zeilennummer (die Datei wird von Hand gepflegt, die Zeile ist die halbe Auskunft); ein Eintrag ohne passendes Finding ist ein **Hinweis** im Report mit `finding_id`, Regel und `bp_key` – er kann bedeuten, dass der Fall behoben ist. In `mdq load` laufen keine Regeln, dort wird „verwaist" deshalb gar nicht erst behauptet. Entschieden von Victor. Verworfen: Findings mit Entscheidung unterdrücken (stumm, Regel 4), das Gedächtnis aus dem Vorlauf lesen, `run_id` in `decision_memory` führen (die Entscheidungen gelten über Läufe hinweg).
- **D-089 · 2026-08-30 · `reason_code` bildet auf die vorhandenen Statuswerte ab; es wird keiner erfunden.** `accepted_risk` → Status `accepted_risk`; `intentionally_separate`, `data_correct` und `not_relevant` → Status `rejected`. Beide Zielwerte stehen so in `logic/finding.schema.json`, und der ursprüngliche Grund bleibt in `decision.reason_code` erhalten – die Unterscheidung geht also nicht verloren, nur weil drei Gründe denselben Status ergeben. Verworfen: einen Status `whitelisted` ergänzen (das Schema kennt vier Endzustände, ein fünfter zwänge UI und Regressionsvergleich zur Anpassung ohne fachlichen Gewinn).
- **D-090 · 2026-08-30 · Der Datenstand kommt aus `--data-as-of`, sonst aus den Posten – und der Report nennt immer beides.** Ohne Angabe gilt das späteste Buchungs- oder Ausgleichsdatum aller Posten im Scope: ein Export kann keine Bewegung enthalten, die nach seinem Ziehungsdatum liegt. Der Wert entscheidet über das Zwölfmonatsfenster und damit über jede Relevanzzahl, deshalb steht er im Run-Report mit seiner Herkunft (`--data-as-of` oder „spätestes Buchungs-/Ausgleichsdatum der Posten") – eine stille Annahme wäre hier besonders teuer. Enthält der Lauf keinen Posten, gibt es nichts abzuleiten und die Angabe ist Pflicht, mit entsprechender Meldung. Der Schalter hängt schon in dieser Aufgabe an `mdq load`, weil die Relevanz ihn braucht; `mdq run` übernimmt ihn unverändert (Aufgabe 4). Verworfen: das heutige Datum als Vorgabe (macht Läufe von der Uhr abhängig und bricht Regel 9), den Datenstand aus `manifest.json` des Demo-Mandanten lesen (den gibt es beim Kunden nicht).

- **D-091 · 2026-08-30 · Die Engine setzt Belegartenlisten als `${doc_types.<Seite>.<Klasse>}` ins Regel-SQL ein – Nachtrag zu D-084, mit einer Korrektur.** Umsetzung der Injektion: Vor der Ausführung ersetzt `executor.rule_sql()` jeden Platzhalter durch die Werteliste aus dem Wörterbuch (`${doc_types.AP.credit_memo}` → `'KG'`); mehrere Klassen lassen sich mit `+` verbinden (`credit_memo+reversal`). Damit liest die Regel weiterhin nur das kanonische Schema und eine Werteliste (Regel 5), und die Liste steht nicht zweimal im Repo. Ein unbekannter Platzhalter und eine **leere** Klasse sind Fehler mit Namen: ein leeres `IN ()` wäre ein SQL-Fehler ohne Auskunft, und eine stillschweigend weggelassene Bedingung wäre schlimmer (Regel 4). Genau darüber schließt sich später der offene Punkt aus D-082: sobald ein Kundenexport eine Stornobelegart zeigt, wird sie unter `reversal` eingetragen und die Regel auf `${doc_types.AP.credit_memo+reversal}` erweitert; der Regel-Klartext („Gutschrift/Storno") bleibt dabei unverändert, weil er das schon immer verlangt hat. **Korrektur an D-084:** dort steht, das Wörterbuch enthalte nur Belegarten des Demo-Mandanten. Das SQL von AP-LEA-001 führte aber seit jeher `('KR','RE')` als Kreditorenrechnung – `RE` ist die Logistik-Rechnung aus MM. Die Umstellung hätte die Regel sonst stillschweigend verengt (Regel 1: eine bestehende, geprüfte Festlegung wird nicht nebenbei geändert), deshalb steht `RE` jetzt unter `AP.invoice`. Auf den Demo-Mandanten wirkt das nicht: dort kommt keine RE-Belegart vor, und die Probe nach der Umstellung ist unverändert **AR-VAL-001 15/15, AR-CON-002 7/7, AP-LEA-001 8/8**. Version und Klartext von AP-LEA-001 bleiben unverändert – es ändert sich nur, woher die Liste kommt. Drei Stolperdrähte in `test_executor.py`: die Platzhalter sind nach dem Einsetzen verschwunden, ein Lauf mit einem Wörterbuch **ohne** `KR` findet das Paar nicht mehr (käme die Liste weiter aus dem SQL, änderte das Wörterbuch nichts), und `credit_memo+reversal` verbindet zwei Klassen. Verworfen: die Listen als DuckDB-Tabelle bereitstellen (Regeln läsen eine Tabelle außerhalb des kanonischen Schemas), Platzhalter über `str.format` (die Regeln enthalten geschweifte Klammern in `to_json({...})`).

- **D-092 · 2026-08-31 · Der Zeitpunkt eines Laufs kommt aus der Uhr, `--created-at` nagelt ihn fest – und keine Laufzeit landet in den Artefakten.** Ein Lauf findet wirklich zu einer Zeit statt, und `created_at` steht in jedem Finding; die Vorgabe ist deshalb die Uhr (UTC, auf Sekunden genau, mit `Z`). `--created-at JJJJ-MM-TTTHH:MM:SSZ` setzt sie fest; eine Angabe ohne Zone oder mit Offset ≠ 0 ist ein Fehler mit Namen, denn der Zeitpunkt gehört zum Lauf, nicht zum Betrachter. Die Zusicherung lautet ab jetzt im Wortlaut: **mit festem `--created-at` byte-identisch; ohne ihn identisch bis auf `created_at` in `run.json` und Findings.** Der Determinismus-Test prüft beide Varianten – mit Schalter byte-gleich, ohne Schalter ist der Diff auf genau dieses Feld beschränkt. Zwei Folgen: **keine gemessene Dauer in den Dateien** – sie wäre der einzige Wert der ganzen Pipeline, der sich bei gleichem Input ändert; die Konsole nennt sie, `run.json` nicht. Und **der Vermerk über ein ersetztes Laufverzeichnis steht nur auf der Konsole** (D-093), denn im `run.json` des zweiten Laufs stünde ein Satz, den der erste nicht hatte – die Byte-Gleichheit wäre dahin. `run_meta.started_at` und `finished_at` tragen beide den Zeitpunkt des Laufs, aus demselben Grund. Die Zeile in `report.txt` („Zeitpunkt: …") zeigt denselben Wert und ist damit die dritte Stelle, an der er ohne Schalter abweicht – das ist keine Ausnahme von der Zusicherung, sondern dasselbe Feld. `SPRINT-3.md` Zeile 80 ist auf diesen Wortlaut präzisiert. Entschieden von Victor. Verworfen: `created_at` aus `data_as_of` ableiten (behauptet eine Uhrzeit, die es nicht gab), die Uhr im Executor (bricht D-028), Dauer in `run.json` (keine Datei wäre je zweimal gleich).
- **D-093 · 2026-08-31 · Der `run_id` benennt die Daten und die Frage, nicht den Code; geschrieben wird atomar, ersetzt wird mit Vermerk.** `run_id = <data_as_of>-<kurzhash>`, der Kurzhash sind die ersten acht Hexstellen eines sha256 über: je Inputdatei Tabellenname und sha256 (nach Tabellenname sortiert), den Scope (Buchungskreise, Seite, Postenfenster, `--decimal-notation`), den Datenstand und den Inhaltshash der Entscheidungsdatei. **Nicht** im Hash: Engine-, Paket- und Wörterbuchversion. Grund: derselbe Datenstand mit derselben Frage ist derselbe Lauf, auch nach einem Engine-Update – die Versionen stehen in `run.json`. Der Scope musste hinein, weil sonst ein Lauf mit `--company-codes 1000` und einer ohne Filter dasselbe Verzeichnis beschreiben und einander stillschweigend überschreiben würden; die Entscheidungsdatei, weil sie den Status der Findings verändert. Geschrieben wird in ein temporäres Verzeichnis `.tmp-<run_id>` und erst nach Erfolg getauscht: ein Abbruch lässt einen vorhandenen Lauf unberührt, und es gibt nie ein halb ersetztes Verzeichnis (bei einem Abbruch entsteht überhaupt kein Laufverzeichnis). Ein vorhandener Lauf wird **ersetzt, aber nicht verschwiegen**: der Vermerk auf der Konsole nennt beide Stände („vorher engine 0.1.0/pack 0.1, Paket-Hash 8c32a98a; jetzt …"), weil die Versionen nicht im Hash stehen und ein alter Lauf sonst still unter einem neuen Regelpaket verschwände. Ist das alte `run.json` unlesbar, sagt der Vermerk genau das. Entschieden von Victor. Verworfen: nur die Input-Hashes (Scope-Kollision), Versionen mitzuhashen (jeder Release erzeugte einen neuen „Lauf" derselben Daten), Abbruch bei vorhandenem Verzeichnis mit `--force` (bei gleichem `run_id` sind Eingabe und Frage identisch – das ist derselbe Lauf).
- **D-094 · 2026-08-31 · `run.json` ist eine Obermenge: der ganze Run-Report **plus** der Lauf-Kopf, den die UI erwartet.** Der UI-Strom liest `run.json` als `RunInfo` (`ui/src/types/finding.ts`) mit genau sechs Feldern: `run_id`, `data_as_of`, `engine_version`, `pack_version`, `tables_loaded`, `company_codes` – und hakt die Datei ohne Prüfung durch; fehlte `company_codes`, scheiterte das Datenstand-Banner. Zwei dieser Felder kannte `RunReport.to_dict()` nicht. `run.json` trägt deshalb beides: die sechs Kopffelder flach oben, dazu `created_at`, `house_currency`, `versions`, `scope`, die Stufen (`files`, `stages`, `canonical`, `relevance`, `decisions`), `rejects`, `rules`, `totals`, `hints`, `has_problems` und `exit_code`. `tables_loaded` steht damit doppelt (auch als `totals.files`) – redundant, aber am erwarteten Platz; das ist billiger als eine UI, die in `totals` graben muss. Ein Test prüft die sechs Felder samt Typ und nennt die UI-Datei, damit die Kopplung sichtbar bleibt. Zum Format: `indent=2`, `ensure_ascii=False`, abschließender Zeilenumbruch (wie das UI-Build-Skript), Reihenfolge überall festgelegt, Beträge als String mit Währung daneben – ein `Decimal`, das bis hierher käme, lässt `json.dump` scheitern statt zu runden (Regel 2). `report.txt` wird mit **fester Breite 100 und ohne Farbcodes** geschrieben: `rich` richtet sich sonst nach der Terminalbreite, und dieselbe Datei sähe auf zwei Rechnern verschieden aus (Regel 9). Zusätzlich beschreibt sich jeder Lauf in der Tabelle `run_meta` (Versionen, Paket-Hash, Datenstand, Input-Hashes, Scope); die DuckDB bleibt in Sprint 3 im Speicher, aber die Tabelle ist die Stelle, an der ein Lauf über sich Auskunft gibt. Verworfen: nur `RunInfo` schreiben (der Report wäre verloren, den Aufgabe 5 und der Kunde brauchen), eine zweite Datei `run-info.json` (zwei Wahrheiten, und die UI lädt genau einen Pfad).
- **D-095 · 2026-08-31 · `company_codes` beschreibt den Inhalt des Laufs, `scope.company_codes` den Filter.** Oben in `run.json` stehen die Buchungskreise, die der Lauf tatsächlich enthält (aus `company_code`, sortiert) – ohne `--company-codes` wäre das Feld sonst leer und das Datenstand-Banner stumm, obwohl der Lauf zwei Buchungskreise umfasst. Der Filter steht daneben unter `scope`, wo eine leere Liste „alle" heißt. Damit ist beides beantwortet und keines geraten. Das UI-Build-Skript leitet `company_codes` heute aus `entity.company_code` der Findings ab – für einen Lauf ohne Findings in einem Buchungskreis wäre das eine andere Zahl; die Engine kennt die richtige. Verworfen: den Filter als `company_codes` ausgeben (leer bei jedem Lauf ohne Einschränkung), das Feld aus den Findings ableiten (ein Buchungskreis ohne Finding verschwände).
- **D-096 · 2026-08-31 · `logic/pack.yaml` trägt die Paketversionen, `versions.pack_hash` den Nachweis darüber.** `pack_version` und `dict_version` sind von Hand gepflegte Nummern in `logic/pack.yaml`; die Engine liest sie, jedes Finding trägt `pack_version`, `run.json` beide. Eine Nummer sagt aber nur, was gemeint war – sie bleibt stehen, wenn jemand eine Regel ändert und das Hochzählen vergisst. Deshalb zusätzlich `versions.pack_hash`: ein sha256 über **alle** Dateien unter `logic/`, sortiert nach relativem Pfad, jeweils Pfad **und** Inhalt (Pfad, damit eine umbenannte Regel den Hash ändert; binär gelesen, damit ein anderes Zeilenende ein anderes Paket ist). Er steht in `run.json` und in der neuen Spalte `run_meta.pack_hash`, geht aber **nicht** in den `run_id` (D-093). Damit lässt sich zu jedem Lauf zeigen, mit welchem Paket er entstanden ist, ohne dass ein Release die Laufkennung verschiebt. Entschieden von Victor. Verworfen: nur die Nummer (nicht überprüfbar), nur den Hash (in der UI eine unlesbare Zeichenkette), die Version aus dem Mapping mitbenutzen (das Mapping ist ein Teil des Pakets, nicht das Paket).
- **D-097 · 2026-08-31 · Exit-Codes: 0 sauber, 1 der Lauf hatte Auffälligkeiten, 2 Abbruch – und ein Hinweis ist keine Auffälligkeit.** Findings sind das Produkt eines Laufs, kein Mangel: 30 saubere Findings sind **Exit 0**. **Exit 1** heißt, der Lauf selbst hatte Auffälligkeiten – Rejects, übersprungene oder fehlgeschlagene Regeln; die Dateien werden trotzdem vollständig geschrieben. **Exit 2** ist der Abbruch (mehrere Hauswährungen, fehlende Pflichtdatei oder -spalte, unlesbares Mapping oder Wörterbuch, fehlerhafte Entscheidungsdatei, leeres Eingabeverzeichnis); dann entsteht kein Laufverzeichnis. Dafür musste `RunReport.has_problems` eingeengt werden: es zählte bisher **jeden** Hinweis mit, und der D-079-Hinweis („`name_norm` bleibt bis Sprint 4 leer") steht in jedem Lauf – der Exit-Code wäre dauerhaft 1 und damit ohne Aussage, genau wie ein dauerhaft roter Test. Hinweise stehen jetzt getrennt unter `hints` in `run.json` und als eigene Zeilen im Report: gesagt wird alles (Regel 4, D-038 bleibt), nur nicht alles gleich laut. `SPRINT-3.md` Zeile 49 ist auf diesen Wortlaut präzisiert. Entschieden von Victor. Verworfen: Findings > 0 als Exit 1 (jeder erfolgreiche Lauf beim Kunden wäre ein CI-Fehlschlag), Hinweise weiter mitzählen.
- **D-098 · 2026-08-31 · Ein ungültiges Finding bricht den Lauf ab; eine kaputte Regel hält die anderen nicht auf.** Zwei Fälle, die der Executor bisher gleich behandelte, werden getrennt: `InvalidFindingError` (neue Unterklasse von `ExecutionError`) für ein Finding, das nicht gegen `logic/finding.schema.json` validiert – der Lauf endet mit Exit 2, weil CLAUDE.md Regel 6 das so verlangt und ein Lauf, der schema-ungültige Findings ausliefert, schlimmer ist als keiner. Jede andere `ExecutionError` (SQL-Fehler, verletzter Ausgabe-Vertrag, fehlender Platzhalter) macht **diese** Regel zu einer fehlgeschlagenen: sie steht mit Grund im Report, die übrigen Regeln laufen weiter, der Lauf endet mit Exit 1. Grund für den Unterschied: eine kaputte Regel ist ein Mangel an einer Prüfung, ein ungültiges Finding ein Mangel am Produkt. Dazu: zwei Regeln dürfen kein Finding teilen – dieselbe `finding_id` aus zwei Regeln bricht den Lauf ab und nennt beide IDs (eine von ihnen braucht eine eigene `finding_key`-Spalte). Verworfen: jede `ExecutionError` abbrechen (eine einzelne kaputte Regel nähme dem Kunden den ganzen Lauf), ein ungültiges Finding als fehlgeschlagene Regel zählen (Regel 6 ist nicht verhandelbar).
- **D-099 · 2026-08-31 · Der Lauf trägt den `finding_key` mit, ohne ihn auszuliefern.** Der Vergleichsschlüssel der Regression ist `(rule_id, bp_key, company_code, finding_key)` (D-068), aber `execute_rule()` warf den `finding_key` weg: er geht nur in den Hash der `finding_id` ein und ist aus dem fertigen Finding nicht mehr herauszulesen. `_execute_rules()` benutzt deshalb `execute_rule_rows()`, und `RunResult` trägt zusätzlich `finding_keys` (`finding_id -> finding_key`) samt der Eigenschaft `finding_rows`, die genau die Form liefert, die `actual_from_findings()` erwartet. Der Schlüssel steht **nicht** in `findings.json`: er gehört nicht ins Finding-Schema, und `findings.json` bleibt byte-identisch zu vorher. Damit vergleicht die Regression denselben Lauf, den auch der Kunde bekommt, statt die Pipeline ein zweites Mal aufzubauen. Verworfen: den Schlüssel aus `entity.documents` zurückrechnen (dann entschiede die Engine, was laut D-068 die Regel entscheidet – und für eine Regel ohne Belegbezug ginge es gar nicht), den Vergleich eine eigene Pipeline fahren lassen (zwei Wahrheiten, und ein Fehler im Lauf bliebe unsichtbar).
- **D-100 · 2026-08-31 · Die scharfe Regression prüft die drei Fehlertöpfe hart und die Hinweistöpfe gegen eine erklärte Menge.** `missing`, `unexpected` und `deviating` müssen leer sein – das ist die Zusicherung, und die Meldung im Fehlerfall ist `Comparison.render()`. Die beiden Hinweistöpfe sind damit aber nicht ungeprüft: `known_open` muss **genau** die zwei AP-LEA-001-Paare mit `from_rule_version: "1.1"` sein (D-054), `early` leer, und `rule_missing` gleich der Menge `NOT_YET_BUILT` im Test – 16 Regel-IDs, ausgeschrieben, **keine Zahl**. Diese Menge ist keine Erwartung an ein Ergebnis, sondern die Erklärung, welche Regeln als Datei existieren; ob sie das Richtige finden, bleibt Sache der drei Fehlertöpfe. Ein Regelpaket aus Aufgabe 6–8 entfernt seine IDs im selben Commit, in dem es die Regeln bringt; am Ende von Sprint 3 muss dort genau `{AR-DUP-001, AP-DUP-001}` stehen. So ist das Endbild aus Aufgabe 5 eine ausdrückliche Zusicherung statt eines Nebenprodukts, und eine versehentlich verschwundene Regeldatei färbt den Test rot, statt still als „noch nicht gebaut" durchzugehen. Verglichen wird der eingecheckte `testdata/demo_mandant` (Wortlaut der Spec); dass er dem Generator entspricht, hält `test_repo_copy_matches_the_generator` fest. Entschieden von Victor. Verworfen: die Zahlen 198 (heute) und 20 (Sprintende) im Test (müssten bei jedem Regelpaket angefasst werden – genau die Gewohnheit, die Regel 1 verhindern soll, und eine falsch gebaute Regel könnte die Zahl zufällig treffen), nur `comparison.ok` prüfen (die Hinweistöpfe blieben unbeaufsichtigt).
- **D-101 · 2026-08-31 · Wörterbücher kommen als SQL-Wertepaare ins Regel-SQL; was das Wörterbuch nicht kennt, wird nicht beurteilt, aber genannt.** Erweiterung von D-091 um eine zweite Form: neben der Werteliste (`${doc_types.AR.invoice}` → `'DR'`) gibt es jetzt Wertepaare – `${vat_patterns.rows}` wird zu `('AT','^ATU\d{8}$'), ('BE',…)` und im Regel-SQL zu `VALUES`, über das die Regel joint. Damit steht `vat_id_patterns.yaml` weiter genau einmal im Repo, und AR-VAL-002 liest nur kanonisches Schema plus eine Werteliste (Regel 5). Einfache Anführungszeichen werden verdoppelt, die Reihenfolge ist sortiert (Regel 9). **Drei Fälle statt zwei** in AR-VAL-002: Präfix mit Muster und Verstoß → Finding; kein Buchstabenpräfix und kein Muster trifft → Finding mit dem Steuernummer-Hinweis (D-058); Buchstabenpräfix **ohne** Muster (US, …) → **kein** Finding, denn ohne Muster ist „falsch" eine Behauptung. Verschwiegen wird dieser dritte Fall trotzdem nicht (Regel 4): die kanonische Stufe zählt die Präfixe ohne Muster und nennt sie als Lauf-Hinweis („12 mit Präfix US – zu diesen Präfixen kennt vat_id_patterns.yaml kein Muster"). Der Hinweis nennt **Präfix und Anzahl, nie einen Wert** – eine USt-IdNr. ist eine Geschäftspartnerangabe (Regel 8). Er sitzt bewusst in der kanonischen Stufe und nicht in der Regel: die Abdeckung des Wörterbuchs gilt für den Lauf, auch wenn die Formatregeln gar nicht ausgeführt werden. Reihenfolge der Prüfung (von Victor festgelegt): erst Muster nach dem Präfix des **Werts**, dann der Steuernummer-Fall – nach dem Sitzland zu prüfen, trüge jedem AR-VAL-001-Fall ein zweites Finding ein (D-058). Nicht-EU-Muster (CH, GB, NO) gelten mit; geprüft wird die Form, nicht die Gültigkeit. Verworfen: die Muster als DuckDB-Tabelle (Regel läse außerhalb des kanonischen Schemas), sie ins SQL zu kopieren (zwei Wahrheiten), unbekannte Präfixe als Finding (Behauptung ohne Grundlage).
- **D-102 · 2026-08-31 · Platzhalterbegriffe stehen in `logic/dictionaries/placeholder_terms.yaml`, getrennt nach Einzelbegriffen und ausgeschriebenen Zusammensetzungen.** AR-VAL-005 (und später AP-VAL-005) fragt dasselbe auf beiden Seiten; im Regel-SQL stünde die Liste zweimal. Die Datei trägt zwei Listen: `terms` (ganze Wörter: test, xxx, dummy, unbekannt, neuanlage, muster, probe, tbd, ???, kein name, n.a.) und `compounds` (testkunde, testfirma, testlieferant, musterfirma, musterkunde, mustermann, dummykunde). Technisch werden beide gleich geprüft – als **ganzes Wort**, ohne Rücksicht auf Groß- und Kleinschreibung; getrennt gelistet, weil jede Zusammensetzung einzeln entschieden wurde. **Keine Präfixregel `Test*`:** „Testo SE" ist ein realer deutscher Hersteller, und eine Regel, die reale Firmen trifft, verbrennt das Vertrauen in den ganzen Lauf. Die Wortgrenze steht nicht als `\b` im Regex: DuckDB benutzt RE2 ohne Lookarounds, und `\b` hätte bei `???` und `n.a.` die falsche Bedeutung – stattdessen wird der Text als `'#' || lower(feld) || '#'` in Grenzzeichen gefasst und der Begriff mit `[^a-z0-9äöüß]` umschlossen. Die Engine setzt den fertigen Regex als `${placeholder_terms.pattern}` ein; die Maskierung der Sonderzeichen passiert in Python, wo `re.escape` sie erledigt, nicht im SQL, wo sie niemand sähe. **Vor dem Bau gemessen** (Auflage Victors): mit der ursprünglichen Liste 8 der 10 erwarteten Konten – `xxx` fehlte, obwohl `CATALOG.md` es im Regeltitel nennt; nach Aufnahme genau 10, kein Zusatztreffer in Namen oder Orten, null auf der Kreditorenseite. Geprüft werden Name **und** Ort: eine naive Teilstringsuche hätte über das Kürzel `n.a.` die Orte Jena, Unna, Pirna, Bologna und Verona getroffen – 38 Debitoren und 28 Kreditoren; mit Wortgrenzen sind es null. Verworfen: die Liste ins Regel-SQL (zweimal gepflegt), eine Präfixregel (trifft reale Firmen), nur den Namen prüfen (der Katalogtitel nennt beide Felder).

<!-- Uebernahme aus ui/NOTES.md, Branch `ui-proto`, beim Merge am 2026-08-31: D-112 bis D-184. -->

- *D-103 und D-104 sind nie vergeben worden: sie waren im Plan zu Aufgabe 6 für die Mehrheitsdefinition und den Fensterbeginn reserviert, standen bei der Umsetzung dann aber als D-108 und D-110 in ihren jeweiligen Commits. Der Nummernkreis bleibt lückenhaft, statt bestehende Nummern zu verschieben – auf sie verweisen Commits und Regelköpfe.*
- **D-105 · 2026-08-31 · AR-VAL-003 zeigt die IBAN ausschließlich maskiert – und der Test sucht danach im ganzen Finding.** Schadensklasse 1 heißt nicht nur „nie Stufe A" (Regel 11), sondern auch: die Bankverbindung selbst gehört nicht in ein Artefakt, das exportiert, in ein Ticket kopiert und in einer UI angezeigt wird. Die Regel liefert deshalb überall `substr(iban_norm,1,4) || ' … ' || substr(iban_norm,-4)` – in `current.value`, in `evidence[].value` und im Titel; `source_summary` nennt gar keinen Wert. Geprüft wird das nicht feldweise, sondern über **jede Zeichenkette jedes AR-VAL-003-Findings** des Laufs mit einem Muster für unmaskierte IBAN (`[A-Z]{2}[0-9]{2}[A-Za-z0-9]{10,}`): ein neues Feld, an das beim Schreiben der Regel niemand denkt, fällt damit auf. Bankverbindungen ohne IBAN (`iban_valid IS NULL`) erzeugen kein Finding – nicht geprüft ist nicht dasselbe wie falsch. Entschieden von Victor. **Aufgelöst am 2026-08-31 (Victor):** die Maskierung gilt, und `logic/examples/findings/F-006-AP-VAL-003.yaml` wird **in Aufgabe 7** auf `iban_masked` nachgezogen – Begründung Schadensklasse 1 und das Maskenformat aus D-069. Das Beispiel zeigte in `current.value` und `evidence[0].value` bis dahin die vollständige IBAN; es bleibt in diesem Commit unangetastet, weil es Victors fachliche Spec für die AP-Zwillingsregel ist. Damit ein maskiertes Finding in SAP trotzdem auffindbar bleibt, trägt es **Bankschlüssel (BANKL) und Bankdetail-ID (BVTYP)** – beide identifizieren die Bankverbindung im Stammsatz, ohne die Kontonummer zu nennen. AR-VAL-003 v1.0 führt diese Felder noch nicht; sie kommen mit AP-VAL-003 in Aufgabe 7 in beide Regeln. Verworfen: nur `current` maskieren (die Evidenz trüge die IBAN weiter), die Maskierung erst in der UI (das Artefakt liegt als Datei vor der UI).
- **D-106 · 2026-08-31 · Welche Regeln gebaut sind, steht an genau einer Stelle im Testcode.** Mit AR-VAL-003 wären fünf Stellen gleichzeitig anzufassen gewesen: `NOT_YET_BUILT`, die Regel-ID-Liste in `test_rules.py`, „3 Regeln" in der CLI-Probe, die Tabelle 15/7/8 in `test_regression.py` und dieselbe Tabelle nochmals als „30 Findings" an drei Stellen in `test_run.py`. Fünf Buchhaltungen über denselben Sachverhalt laufen auseinander, und jede einzelne verführt dazu, eine Zahl anzupassen, statt eine Abweichung zu klären (Regel 1). Deshalb: `NOT_YET_BUILT` zieht in `engine/tests/conftest.py` und ist die **einzige** Aufzählung; `test_rules.py` leitet die erwartete Regelliste aus dem Verzeichnis ab, die CLI-Probe die Zahl aus `load_rules()`; `test_regression.py` rechnet die Sollzahl je Regel aus `expected_findings.yaml` abzüglich `known_open`; `test_run.py` prüft nur noch, dass Report und `findings.json` dieselbe Zahl nennen und dass jedes Finding aus einer gebauten Regel stammt. Die Zahl je Regel steht damit nur noch in `expected_findings.yaml` – der Datei, die per Regel 1 ohnehin nie angepasst wird. Verworfen: die fünf Stellen je Regelcommit mitzuziehen (Fleißarbeit mit eingebauter Fehlerquelle).
- **D-107 · 2026-08-31 · Schwellen einer Regel stehen als benannter Parameter im Regelkopf, nicht als Zahl im SQL.** Neuer optionaler Kopfblock `parameters:`; im SQL steht `${params.<name>}`, die Engine setzt den Wert vor der Ausführung ein (dieselbe Stelle wie die Belegartenlisten, D-091). AR-COM-002 braucht `min_invoices: 3` – ab wie vielen Rechnungen eine Mehrheit als Soll taugt. Als `>= 3` im SQL wäre die Zahl weder im Katalog noch im Klartext auffindbar und niemand könnte sie beim Onboarding begründet ändern; im Kopf steht sie mit Kommentar über der Regel und wandert automatisch in jede Ansicht, die den Kopf zeigt. Erlaubt sind **nur Zahlen**: ein Text müsste quotiert werden, und eine Quotierung, die niemand sieht, ist eine Einladung zur Injektion – braucht eine Regel später einen Text, wird das ausdrücklich erlaubt statt stillschweigend. Ein `${params.x}` ohne Eintrag im Kopf ist ein Fehler mit Namen (Regel 4), keine Vorgabe. Verworfen: die Schwelle als Spalte im Katalog (der Katalog ist Victors Arbeitsstand, nicht die ausgeführte Wahrheit), globale Schwellen in einer Konfigurationsdatei (eine Schwelle gehört zu genau einer Regel, sonst verschiebt ein Kunde ungewollt eine zweite).
- **D-108 · 2026-08-31 · AR-COM-002: Soll nur bei echter Mehrheit, sonst Finding ohne Soll.** Soll ist die Zahlungsbedingung auf **mehr als der Hälfte** der Rechnungsbelege des Debitors im Buchungskreis, bei mindestens `min_invoices` Rechnungen – dann Stufe B (beobachtetes Verhalten ist laut CONCEPT §4 eine starke Quelle). Sonst entsteht das Finding trotzdem, aber mit Stufe C und ohne `proposed.value`: die leere ZTERM ist der Befund, das Soll ist die Zugabe. Der häufigste Wert allein genügt nicht – der Demo-Mandant führt den Fall vor, in dem sich zehn Rechnungen als 3/3/2/2 auf vier Bedingungen verteilen: es gibt einen häufigsten Wert, aber keine Aussage. Die Zahlen bestätigen die Trennung ohne Nachhilfe: 15 Konten mit Mehrheit (Stufe B), 5 ohne (Stufe C) – genau die Aufteilung, die `defects.yaml` mit `dominant: true|false` beschreibt. Ein Gleichstand wird nach Schlüssel aufgelöst, damit die Auswahl reproduzierbar bleibt (Regel 9); eine Mehrheit ist ein Gleichstand ohnehin nie. Entschieden von Victor. Verworfen: häufigster Wert ohne Mehrheitsschwelle (macht 3 von 10 zum Soll), Finding nur bei ableitbarem Soll (dann verschwänden fünf leere Felder stumm, Regel 4).
- **D-109 · 2026-08-31 · Fund im Wörterbuch: der Präfix `NO` war in YAML kein Text, sondern `False`.** `logic/dictionaries/vat_id_patterns.yaml` führte Norwegen als `NO: "^NO\d{9}MVA$"`. YAML 1.1 liest `NO` als Boolean – der Schlüssel hieß `False`, das Muster war unerreichbar, und eine norwegische USt-IdNr. wäre stumm „ohne Muster" gewesen. Der Schlüssel steht jetzt in Anführungszeichen mit Begründung als Kommentar; der Parser lehnt zusätzlich jeden Schlüssel ab, der nicht aus zwei Großbuchstaben besteht – derselbe Fehler fällt damit künftig beim Laden auf und nicht erst in den Daten. Auf den Demo-Mandanten wirkt es nicht (keine norwegische Nummer); gefunden beim Bau von AR-VAL-002.
- **D-110 · 2026-08-31 · Der Beginn des Postenfensters kommt vom Lauf, nicht aus der Regel – und der Lauf sagt, woher.** AR-HYG-001 verlangt „kein Posten im Postenfenster **und** angelegt vor Fensterbeginn" (D-049). Der Fensterbeginn ist eine Eigenschaft des Laufs: `scope.item_window_from`, wenn gesetzt, sonst das früheste Buchungsdatum der geladenen Posten – der Export *ist* das Fenster. Die Engine setzt ihn als `${scope.item_window_from}` ins Regel-SQL ein (dieselbe Stelle wie Belegarten und Schwellen, D-091/D-107), und `run.json` sowie `report.txt` nennen ihn mit Herkunft: „Postenfenster ab: 2024-09-02 (frühestes Buchungsdatum der geladenen Posten)" – dieselbe Sorgfalt wie beim Datenstand (D-090), denn beide Zahlen entscheiden über Findings. Enthält ein Lauf **keinen** Posten, gibt es keinen Fensterbeginn: eine Regel mit diesem Platzhalter schlägt dann mit Namen fehl, statt still ein Datum zu erfinden (Regel 4). Warum nicht in der Regel rechnen: ein `(SELECT min(posting_date) FROM fi_item)` im SQL wäre eine stille Ableitung, die in keinem Artefakt auftaucht – niemand könnte einem Finding ansehen, gegen welches Fenster es entstand. Warum nicht `run_meta` lesen: das ist die Selbstauskunft des Laufs, keine Fachtabelle; Regeln lesen das kanonische Fachmodell (Regel 5). Entschieden von Victor. Verworfen: Ableitung in der Regel, Bedingung ganz weglassen (kollidiert mit AR-HYG-002, das Neuanlagen im Fenster meint).
- **D-111 · 2026-08-31 · AR-HYG-001 entscheidet nicht, sondern legt drei Optionen vor.** Stufe `decision`, Aktion `decision`, kein `proposed.value` – stattdessen `options`: Löschvormerkung mit Archivierung, Konto behalten, nur Buchungssperre. Grund: die Daten zeigen zuverlässig, dass ein Konto ruht, aber nicht, ob es gelöscht werden darf – das entscheiden Aufbewahrungsfrist (§ 147 AO, zehn Jahre) und Kundenbeziehung. Der Katalog erlaubt, dass eine Policy die Regel zur Laufzeit gemeinsam auf Stufe A und `mass_change` hebt; in Sprint 3 gibt es keine Policy, es bleibt beim konservativen Default. Ohne Anlagedatum gilt ein Konto als vor dem Fenster angelegt (D-086): „nie bebucht" ist die stärkere Behauptung und braucht ein Datum. 40 Findings auf dem Demo-Mandanten, genau die 40 Konten ohne einen einzigen Posten.
- **D-112 · 2026-08-30 · UI-Strom · Ab 30.08.: UI ist Produktcode, kein Prototyp.** Freigabe Victor. Daraus
  folgen drei Festlegungen: Datenzugriff hinter einem `FindingsSource`-Adapter (Datei,
  später Lauf-Verzeichnis oder API, ohne dass die Screens es merken); Export-/Import-Format
  für Entscheidungen als spätere Persistenz festziehen (was Aufgabe 7 schreibt, muss sich
  auch wieder einlesen lassen); Tabelle virtualisierungsfähig schneiden (Zeilenhöhe und
  Zeilenzugriff so, dass ein Fenster über 10.000 Findings ohne Umbau nachrüstbar ist).
  Grund: der Satz „das Design bleibt, der Code ist austauschbar" aus der Spec gilt nicht
  mehr – was hier entsteht, wird weitergepflegt. Verworfen: Prototypen-Freiheiten jetzt
  und Aufräumen später.
- **D-113 · 2026-08-30 · UI-Strom · Tailwind v4 statt v3.** Grund: der aktuelle `shadcn`-CLI initialisiert
  gegen v4 (`@tailwindcss/vite`, Theme über CSS-Variablen in `src/index.css`, keine
  `tailwind.config.js`); v3 hieße gegen den Strom des Generators arbeiten.
  Verworfen: Tailwind v3 mit klassischer Config.
- **D-114 · 2026-08-30 · UI-Strom · Findings-Typen von Hand geschrieben** (`src/types/finding.ts`) statt
  `json-schema-to-typescript`. Grund: das Schema lebt von drei `allOf`-Bedingungen
  (Schadensklasse 1 nie Stufe A; Stufe A/B brauchen ein Soll; `mass_change` nur Stufe A),
  die ein Generator zu breiten Typen verflacht. Driftschutz: `src/types/finding.enums.test.ts`
  gleicht alle neun Enum-Listen zur Testzeit gegen `logic/finding.schema.json` ab (nur lesend).
  Verworfen: Generator als Dev-Dependency.
- **D-115 · 2026-08-30 · UI-Strom · Beträge auch im Formatierer als String.** `src/lib/format.ts` arbeitet
  rein string-basiert (Regex + Dreiergruppierung); gerechnet wird über `bigint`-Cents
  (`parseCents`/`sumEur`). Kein `Number`, kein `parseFloat`, keine Rundung. Grund:
  CLAUDE.md Regel 2 endet nicht an der UI-Grenze. Verworfen: `Intl.NumberFormat`, weil
  es den String durch einen `number` schleusen müsste.
- **D-116 · 2026-08-30 · UI-Strom · Kein Locale- und kein Zeitzonen-Bezug in der Formatierung.** `formatDate`
  und `formatDateTime` zerlegen den ISO-String per Regex, ohne `Date`. Zeitstempel werden
  als UTC ausgewiesen („30.08.2026 09:15 UTC"). Grund: Regel 9 – derselbe Lauf muss auf
  jedem Rechner gleich aussehen; der Zeitstempel gehört zum Lauf, nicht zum Betrachter.
- **D-117 · 2026-08-30 · UI-Strom · `formatKey` wurde zur Komponente `src/components/Key.tsx`.** Die Spec
  nennt es „Monospace-Komponente"; als JSX kann es nicht in die `.ts`-Datei `format.ts`,
  die rein und testbar bleiben soll.
- **D-118 · 2026-08-30 · UI-Strom · `tables_loaded: 0` im abgeleiteten `run.json`.** Das UI kennt die
  Ladeschicht nicht und erfindet keine Zahl; die echte liefert später `runs/<run_id>/run.json`
  der Engine. `company_codes` wird dagegen aus `entity.company_code` abgeleitet
  (eindeutig, sortiert → `["1000","2000"]`).
- **D-119 · 2026-08-30 · UI-Strom · Betrag und Währungszeichen mit gewöhnlichem Leerzeichen** („32.000,00 €"),
  nicht mit geschütztem. Grund: so steht es in der Spec und ist in Tests und Diffs sichtbar;
  gegen Umbruch hilft CSS. Offen, falls im Buchhalter-Test störend.
- **D-120 · 2026-08-30 · UI-Strom · Vitest ohne DOM-Umgebung** (`environment: 'node'`). Getestet werden laut
  Spec Formatierer und später der Reducer – beides reine Funktionen. Verworfen: jsdom +
  Testing Library als zusätzliche Abhängigkeiten.
- **D-121 · 2026-08-30 · UI-Strom · TanStack Table v9 statt v8.** `npm install @tanstack/react-table` liefert
  9.2.4 mit neuer API (`tableFeatures({})`, `useTable`, `table.FlexRender`); die aus v8
  bekannten `useReactTable`/`getCoreRowModel` gibt es nur noch als `legacy`-Einstieg.
  Genutzt wird der Kern-Funktionssatz ohne Sortier-, Filter- oder Paginierungs-Feature –
  das erledigen eigene Funktionen. Verworfen: Pin auf v8, verworfen: der `legacy`-Pfad.
- **D-122 · 2026-08-30 · UI-Strom · Sortieren, Filtern und Suchen bleiben eigene reine Funktionen**
  (`src/lib/select-findings.ts`), nicht Sache der Tabelle. Grund: Regel 9 – die
  Reihenfolge ist so im Test festgenagelt statt vom Bibliotheksverhalten abhängig.
  Beträge werden über `parseCents` als `bigint` verglichen, nie über `Number`. Jede
  Sortierung endet auf Schwere absteigend und dann `finding_id`, damit Gleichstand nie
  dem Zufall überlassen bleibt. Findings ohne `impact_eur` zählen als 0.
- **D-123 · 2026-08-30 · UI-Strom · Filter einwertig je Dimension** („Alle" plus ein Wert), Dimensionen
  wirken als UND. Freigabe Victor. Grund: ruhige Leiste, reicht für die Fragen der
  Buchhalter. Verworfen: Mehrfachauswahl je Dimension.
- **D-124 · 2026-08-30 · UI-Strom · Auswahl wird bei Tab-, Filter- und Suchwechsel zurückgesetzt.** Grund:
  eine Tastaturmarke auf einer ausgeblendeten Zeile ist unsichtbar; `J` beginnt danach
  wieder oben. Beim Sortieren und beim Schließen des Drawers bleibt sie stehen.
- **D-125 · 2026-08-30 · UI-Strom · Deutsche Beschriftungen der Enum-Werte in `src/lib/labels.ts`.** Das
  Glossar nennt die Feldnamen, aber keine deutschen Wörter für die Werte. Die Kategorien
  folgen `docs/CONCEPT.md` Abschnitt 3 (Vollständigkeit/Validität/Konsistenz/Hygiene/
  Risiko); `duplicate` → „Dublette". `leakage` → „Geldabfluss" ist hier gesetzt und steht in
  keinem Dokument; **am 2026-08-30 als Glossar-Rückmeldung an die Engine-Session (`main`)
  übergeben**, bis zu einer Antwort bleibt es bei „Geldabfluss". `labels.test.ts` erzwingt, dass jede
  Enum-Konstante genau eine Beschriftung hat.
- **D-126 · 2026-08-30 · UI-Strom · Explorer-Zustand liegt in `App.tsx`**, nicht im Explorer. Grund: Filter,
  Suche und Auswahl überleben so einen Wechsel auf Dashboard und zurück.
- **D-127 · 2026-08-30 · UI-Strom · Rauchtest per `renderToStaticMarkup`** statt jsdom + Testing Library
  (`FindingsExplorer.render.test.tsx`, `vitest`-`include` um `*.test.tsx` erweitert).
  Er beantwortet, was Typprüfung und Reducer-Tests offenlassen: dass Zeilen, Beträge und
  Leerzustände wirklich im Markup ankommen. Grenze: Portale (Drawer, Select-Liste,
  Tooltip) rendert `renderToStaticMarkup` nicht – die Review-Karte prüft Aufgabe 3.
  Ergänzt die Entscheidung „Vitest ohne DOM-Umgebung", widerspricht ihr nicht: es bleibt
  bei `environment: 'node'` und ohne neue Abhängigkeit.
- **D-128 · 2026-08-30 · UI-Strom · Aktion → Status: Übernehmen und Zuweisen setzen `in_progress`.**
  Freigabe Victor. „Übernehmen" heißt „freigegeben – Umsetzung offen", „Zuweisen" heißt
  „zugewiesen"; beide sind `in_progress`, nur der Text unterscheidet sie
  (`DECISION_STATUS_LABELS`, `StatusBadge` mit `label`). Ablehnen setzt `rejected`, mit
  Grund „Risiko akzeptiert" dagegen `accepted_risk`. **`done` vergibt das UI nie** – das
  entscheidet der nächste Lauf, in dem das Finding nicht mehr auftaucht. Verworfen:
  Übernehmen → `done` (behauptet eine Umsetzung, die im SAP noch aussteht).
- **D-129 · 2026-08-30 · UI-Strom · Entscheidungen liegen als Überlagerung über den Findings.**
  `src/state/decisions.ts` hält `finding_id → Entscheidungssatz`; `applyDecisions` legt
  Status und `decision` über eine Kopie, die geladenen Findings bleiben unverändert
  (Analogie zu Regel 3). Das Ergebnis bleibt schemakonform: `decision` bekommt nur
  `by`, `at`, `reason`, `reason_code`. Aufgabe 7 exportiert die Map direkt.
- **D-130 · 2026-08-30 · UI-Strom · `decision.at` kommt aus einer injizierbaren Uhr.** `createDecision(input, now)`
  – der Zeitstempel ist der einzige Wert im UI, der nicht aus den Daten folgt; im Test
  steht er fest (Regel 9). Verworfen: `new Date()` mitten im Rendering.
- **D-131 · 2026-08-30 · UI-Strom · Bearbeiter steht im Datenstand-Banner, nicht in einem Dialog.**
  `decision.by` ist im Schema Pflicht, also wird der Name einmal je Sitzung eingetragen;
  ohne ihn sind Übernehmen, Ablehnen und Zuweisen gesperrt (mit Hinweis, „Später" bleibt
  frei). Nur im Speicher, kein `localStorage`. Verworfen: Namensabfrage beim ersten Klick
  (ein dritter Dialog für eine Angabe, die einmal gilt).
- **D-132 · 2026-08-30 · UI-Strom · Die Review-Karte hängt in keinem Portal.** `ReviewCard` trägt Kopf,
  Abschnitte, Aktionen und die Tastatur; `ReviewDrawer` ist nur noch Sheet-Hülle mit
  zugänglichem Namen. Damit prüft `ReviewCard.render.test.tsx` alle sechs Beispiele mit
  `renderToStaticMarkup` – die in Aufgabe 2 notierte Grenze ist damit aufgehoben, ohne
  jsdom und ohne neue Abhängigkeit.
- **D-133 · 2026-08-30 · UI-Strom · Optionen einer Entscheidung sind wählbar, und die Wahl wird festgehalten.**
  Ohne gewählte Option ist „Übernehmen" gesperrt (Tooltip „Erst eine der Optionen wählen"),
  mit Wahl steht sie als Grund im Entscheidungssatz („Option gewählt: …"). Die Spec nennt
  die Optionen „wählbar"; ohne Wirkung wäre die Wahl Dekoration.
- **D-134 · 2026-08-30 · UI-Strom · „Später" schreibt nichts, und Entscheidungen sind zurücknehmbar.**
  „Später" springt nur zum nächsten offenen Finding. Zu jeder getroffenen Entscheidung
  steht „Zurücknehmen" auf der Karte – ohne Persistenz wäre ein Fehlgriff sonst endgültig.
- **D-135 · 2026-08-30 · UI-Strom · Sprung nach der Entscheidung: `nextOpenId`.** Nächstes offenes Finding
  hinter der Marke, sonst das erste offene von oben, sonst schließt die Karte. Reine
  Funktion im Reducer, damit die Reihenfolge im Test steht und nicht im Klickverhalten.
- **D-136 · 2026-08-30 · UI-Strom · `formatMoney(amount, currency)` statt `formatEur` überall dort, wo ein
  `impact_eur` gezeigt wird.** Die Währung steht im Finding und gehört neben den Betrag
  (Regel 2); unbekannte Währungen behalten ihren Code. Nur `formatEur` bliebe stumm falsch,
  sobald ein Lauf CHF liefert.
- **D-137 · 2026-08-30 · UI-Strom · Die Drawer-Breite läuft über `--sheet-width`, nicht über `className`.**
  Die Breite steht in `sheet.tsx` als `data-[side=right]:sm:max-w-…`; ein `sm:max-w-3xl`
  am Aufruf trägt diese Varianten nicht, `tailwind-merge` sieht keinen Konflikt und lässt
  beide Klassen stehen – im erzeugten CSS gewinnt die Regel mit den Varianten, weil sie
  später steht. Die Review-Karte war deshalb 24rem breit statt der angeschriebenen 48rem.
  `sheet.tsx` liest jetzt `max-w-[var(--sheet-width,24rem)]`, `ReviewDrawer` setzt 44rem
  (≈700 px). Verworfen: `!max-w-*` mit `!important`, verworfen: Radix-Content selbst bauen.
- **D-138 · 2026-08-30 · UI-Strom · Ist | Soll bricht nach der Kartenbreite um, nicht nach der Fensterbreite.**
  Der Scrollbereich der Karte ist `@container`, die zwei Spalten erscheinen ab `@min-[38rem]`
  (≈19rem je Spalte, genug für einen ganzen Satz). `sm:` maß das Browserfenster und damit
  das Falsche – im Drawer sagt es nichts über den vorhandenen Platz. Container-Queries sind
  in Tailwind v4 im Kern, keine neue Abhängigkeit.
- **D-139 · 2026-08-30 · UI-Strom · Drei Fälle rechts statt zwei: Soll / Empfehlung / kein Soll.** Freigabe
  Victor. `proposed.value` gesetzt → „Soll" mit Wert; nur `proposed.display` → „Empfehlung"
  mit dem Satz; weder noch → „Soll" mit „Kein Soll ermittelbar – Entscheidung/Prüfung".
  Grund: ein Handlungssatz ist ein Soll, nur keines, das in ein Feld passt; der feste
  Hinweis behauptete dort Ratlosigkeit, wo eine Empfehlung steht. F-9d0b3f6a1c7e (nur
  Optionen) behält den Hinweis wie in der Spec.
- **D-140 · 2026-08-30 · UI-Strom · Überschrift der Evidenzkarte ist die Referenz, der Quellentyp steht klein
  darunter.** Freigabe Victor. „Regelprüfung" (`deterministic`) beschreibt die Hälfte aller
  Einträge und sagt nichts; `1000/2026/1900004411` sagt, worum es geht. Die Referenz steht
  wörtlich da – ein Wort wie „Beleg" davor stünde in keinem Feld; dafür liegt der Vorschlag
  `evidence.reference_kind` bei der Engine-Session.
- **D-141 · 2026-08-30 · UI-Strom · Nach einer Entscheidung sind Übernehmen, Ablehnen und Zuweisen gesperrt.**
  Freigabe Victor. Ein zweiter Klick würde die erste Entscheidung stillschweigend
  überschreiben; der Weg zurück heißt „Zurücknehmen". Die Tastatur (`A`/`R`/`Z`) ist
  mitgesperrt, sonst umginge sie die Knöpfe. „Später" bleibt aktiv: es schreibt nichts,
  es springt nur zum nächsten offenen Finding.
- **D-142 · 2026-08-30 · UI-Strom · „Findings-Datei laden" liegt im Datenstand-Banner.** Freigabe Victor.
  Die Datei wird nur im Speicher gehalten (kein `localStorage`), der Lauf-Kopf wird wie
  im Build-Skript abgeleitet. Unbrauchbare Dateien brechen mit Grund ab statt halb zu
  erscheinen (Regel 4); die Meldungen nennen nur Feldnamen, Regel-IDs und `finding_id`,
  nie Geschäftspartnerdaten (Regel 8).
- **D-143 · 2026-08-30 · UI-Strom · Der Dubletten-Vergleich ersetzt Ist | Soll, statt ihn zu ergänzen.**
  Freigabe Victor. Bei `category: duplicate` steht in `current.value` kein Ist-Wert, sondern
  eine Liste von Konten; „Ist" und „Soll" nebeneinander beantworten die Frage nicht, die hier
  ansteht („wo unterscheiden sie sich"). `proposed.display` und `source_summary` bleiben als
  Kasten „Soll" unter der Tabelle stehen – sie gingen sonst verloren.
- **D-144 · 2026-08-30 · UI-Strom · Aus Prosa werden keine Vergleichszeilen gelesen.** Freigabe Victor. Die
  Spec verlangt neun Felder je Konto; strukturiert vorhanden sind drei (aus `current.display`).
  Land, USt-ID, IBAN, Zahlungsbedingung, offene Posten und letzte Zahlung stecken als Fließtext
  in `proposed.display` und in Evidenz-Notizen – und dort jeweils nur für **ein** Konto.
  „45.210 € OP" in eine Zeile „Offene Posten" zu übernehmen hieße, dem einen Konto einen Betrag
  zuzuschreiben und dem anderen keinen: geraten, nicht gelesen. Die sechs Felder werden unter
  der Tabelle als Platzhalter benannt (Regel 4: nichts stumm verwerfen), nicht als leere Zeilen
  gezeigt – sechs Zeilen mit „–" sähen nach einer Lücke im Mandanten aus, es ist eine im Schema.
- **D-145 · 2026-08-30 · UI-Strom · Konten werden über den normalisierten Schlüssel zugeordnet, nicht über die
  Position.** `entity.bp_key` trägt das Rollenpräfix (`C:0000100234`), `current.value` und
  `proposed.value` tragen es nicht. `normalizeAccount` streift Präfix und führende Nullen ab
  (nur zum Vergleich – angezeigt wird der Schlüssel unverändert, Regel 2). Dass das erste
  Segment zum ersten Konto gehört, steht nirgends; lässt sich die Zuordnung nicht belegen,
  gibt `buildDuplicateComparison` `null` zurück und die Karte zeigt den gewohnten
  Ist|Soll-Abschnitt. Eine halbe Tabelle wäre schlechter als gar keine.
- **D-146 · 2026-08-30 · UI-Strom · Match-Chips kommen aus `evidence.value`, nicht aus der `note`.** Die Evidenz
  mit `source_type: model` trägt in `value` die Gründe in Listenform
  („name_norm gleich, street_norm gleich, postal_code gleich" → drei Chips, so die Abnahme);
  die `note` ist ein Satz über die Normalisierung und steht darunter.
- **D-147 · 2026-08-30 · UI-Strom · Die Adresszeile wird nur zerlegt, wenn sie sich bei *allen* Konten in genau
  drei Teile teilt.** Sonst steht der Text als eine Zeile „Angabe" wörtlich da. Gemischt wird
  nicht – sonst stünde in einer Spalte ein Name, wo in der anderen die ganze Anschrift steht.
  `missingFields` folgt daraus, statt fest zu stehen: alles aus der Feldliste der Spec, wozu
  keine Zeile entstanden ist.
- **D-148 · 2026-08-30 · UI-Strom · Die Belegpaar-Ansicht steht vor Ist|Soll und ersetzt es nicht.** Freigabe
  Victor. Anders als bei der Dublette steht in `current` ein echtes Feld (`BSAK.XBLNR`), und
  der fehlende Bindestrich zwischen „RE-4711" und „RE4711" ist der Kern des Findings – Ist|Soll
  bleibt also stehen. Die erste Frage am Schreibtisch lautet aber „welche zwei Belege", nicht
  „welches Feld"; deshalb steht die Belegansicht darüber. Verworfen: Belegpaar zwischen Ist|Soll
  und Evidenz, verworfen: Belegpaar an Stelle von Ist|Soll wie bei der Dublette.
- **D-149 · 2026-08-30 · UI-Strom · Beleg und Evidenz werden über die Referenz verbunden, nicht über die Position.**
  `entity.documents` trägt nur den Schlüssel; Referenz, Datum und Betrag stehen im Text der
  Evidenz, deren `reference` `"<BUKRS>/<GJAHR>/<BELNR>"` lautet. Die Zuordnung ist damit belegt
  (anders als bei der Dublette, wo sie geraten gewesen wäre). Zweiter Versuch: die Belegnummer
  als **ganzes** Segment der Referenz – als Teilzeichenkette fände `1900004411` auch
  `11900004411`. Findet ein Beleg keine Evidenz, gibt `buildDocumentPair` `null` zurück und die
  Karte sieht aus wie jede andere; zwei halbe Belegkarten sind schlechter als keine.
- **D-150 · 2026-08-30 · UI-Strom · Der Evidenztext wird zerlegt, aber alles oder nichts.** Aus „RE-4711, Belegdatum
  01.03.2026, bezahlt 28.03.2026" werden drei Felder – nur dann, wenn das Muster bei **jedem**
  Beleg greift; sonst steht bei allen der Satz wörtlich da. Dieselbe Regel wie beim Adress-Split
  in Aufgabe 4: sonst stünde auf einer Karte ein Feldraster und auf der anderen ein Satz. Der
  Widerspruch zu „aus Prosa werden keine Vergleichszeilen gelesen" ist keiner – dort war die
  Zuordnung zum Konto ungeklärt, hier nennt die Referenz den Beleg.
- **D-151 · 2026-08-30 · UI-Strom · Der Betrag je Beleg wird benannt, nicht zugeschrieben.** Freigabe Victor.
  32.000,00 € ist die Euro-Wirkung des Findings und steht als Prosa in `current.display`
  („Zwei bezahlte Rechnungen über 32.000,00 EUR"); dass **jeder einzelne** Beleg über diesen
  Betrag lautet, steht in keinem Feld. Unter den Karten steht deshalb ein Satz „Platzhalter:
  Betrag je Beleg …" (Regel 4). Die Abnahme „F-003 vollständig" ist damit bis zur
  Schema-Erweiterung formal knapp verfehlt.
- **D-152 · 2026-08-30 · UI-Strom · Die Quellenlage steht bei der Belegansicht nur einmal.** `source_summary` ist
  der Fuzzy-Grund und gehört unter die Karten („Warum dieses Paar"); `CurrentProposed` bekommt
  dafür die Prop `omitSourceSummary`. Verworfen: denselben Satz zweimal auf derselben Karte
  stehen lassen – zwei Vorkommen lesen sich wie zwei Aussagen.
- **D-153 · 2026-08-30 · UI-Strom · Das Evidenz-Panel bleibt vollständig.** Die Belegansicht verbraucht keine
  Einträge, sie liest sie nur mit; „Evidenz (3)" zeigt weiterhin alle drei. Die Belegkarte ist
  die Lesehilfe, das Panel die Liste, gegen die geprüft wird. Verworfen: Einträge oben
  „aufbrauchen" – dann verschwände das Panel bei F-c41d7e9b2a60 ganz und mit ihm die Zusage aus
  Aufgabe 3, dass dort jede Quelle steht.
- **D-154 · 2026-08-30 · UI-Strom · `line_item` im Typ optional.** Das Schema führt es nicht unter `required`
  (`entity.documents[].required = [company_code, fiscal_year, document_no]`), der Typ verlangte
  es. Jetzt `line_item?: string | null` – der Vertrag ist das Schema, nicht die Beispieldatei.
- **D-155 · 2026-08-30 · UI-Strom · Der Datenzugriff liegt hinter `FindingsSource` (`src/sources/`).** Aufgabe 5b,
  Punkt 1. `src/lib/load-run.ts` ist aufgelöst in `sources/findings-source.ts` (Vertrag),
  `sources/parse.ts` (Prüfen und Ableiten, rein), `sources/{build,file}-source.ts` und
  `sources/index.ts`. Der naheliegende Name `src/data/` geht **nicht**: die Wurzel-`.gitignore`
  ignoriert `data/` auf jeder Ebene („Daten – niemals einchecken"), die Dateien wären
  unbemerkt nicht im Commit gelandet. Ein `!`-Ausnahme in `ui/.gitignore` wäre ein Loch in
  genau der Regel, die verhindert, dass Daten eingecheckt werden – dafür ist der Ordnername
  zu wenig wert.
  Zum Vertrag gehört mehr als die Signatur: jede Implementierung führt ihre Daten durch
  `parse.ts` (dieselben Prüfungen für Datei und später API), Fehler verlassen eine Quelle
  **immer** als `LoadError` mit deutscher Meldung (Regel 4), Meldungen nennen nur Feldnamen,
  Regel-IDs und `finding_id` (Regel 8). `DEFAULT_SOURCE` in `sources/index.ts` ist die einzige
  Stelle, die ein Backend austauscht. Verworfen: eine Lauf-Auswahl (`listRuns`) in derselben
  Schnittstelle – das ist ein eigener Vertrag und ein eigenes Feature (siehe offene Punkte).
- **D-156 · 2026-08-30 · UI-Strom · Der Quellenwechsel läuft über Zustand, nicht über einen zweiten Aufruf.**
  `App` hält `source` (was gelten soll) und `loaded` (was zu sehen ist, **mitsamt der Quelle,
  aus der es stammt**). Sonst stünde nach einer unbrauchbaren Datei ihr Name im Banner über
  den alten Daten. Der Ladeeffekt hängt allein an `source` und bricht mit `AbortController`
  ab, damit ein schneller Wechsel nicht das alte Ergebnis nachschiebt.
- **D-157 · 2026-08-30 · UI-Strom · `decisions.json` ist ein versionierter Umschlag, kein nacktes Array.**
  Aufgabe 5b, Punkt 2. `{format: "mdq.decisions", format_version: 1, run_id, data_as_of,
  engine_version, pack_version, exported_at, exported_by, decisions: [...]}`; Vertrag in
  `src/types/decisions-file.ts`, Umsetzung in `src/lib/decisions-io.ts`. Versionsregel:
  zusätzliche optionale Felder lassen die Version stehen (ein älterer Leser nennt sie als
  unbekannt, Regel 4), alles was einen älteren Leser falsch verstehen ließe erhöht sie; ein
  Leser nimmt genau die Versionen, die er kennt. `sample_reviewed` (Aufgabe 7) ist im Vertrag
  benannt und heute **nicht** geschrieben. Die Sätze stehen nach `finding_id` sortiert –
  gleicher Stand, gleiche Datei bis aufs Byte (Regel 9). Verworfen: das Array aus der Spec
  ohne Umschlag – ohne Lauf-Kopf ist beim Einlesen nicht prüfbar, wozu die Datei gehört.
- **D-158 · 2026-08-30 · UI-Strom · Der Import ersetzt den Stand der Sitzung, mit Rückfrage.** Freigabe Victor.
  „Gestern weitergearbeitet" ist ein Zustand, nicht die Summe zweier. Liegen lokale
  Entscheidungen vor, fragt `ImportDecisionsDialog` vorher – mit dem Bericht **vor** der
  Übernahme und dem Hinweis, erst zu sichern. Zusammenführen zweier Stände ist als späterer
  Punkt notiert, nicht gebaut.
- **D-159 · 2026-08-30 · UI-Strom · Ein anderer `run_id` in der Entscheidungsdatei ist eine Warnung, kein
  Abbruch.** Freigabe Victor. `finding_id` ist deterministisch (Regel 9), ein Finding kann im
  nächsten Lauf denselben Schlüssel tragen. Angewandt wird nur, was ein Finding im geladenen
  Lauf findet; der Bericht sagt immer zuerst, wie viele das sind und wie viele nicht, und
  nennt die übrigen `finding_id`s einzeln (Regel 4). Abgebrochen wird nur, wenn die Datei als
  Ganzes nicht taugt: falsches `format`, unbekannte `format_version`, kaputter Satz.
- **D-160 · 2026-08-30 · UI-Strom · `exported_by` füllt den Bearbeiter nur, wenn dort nichts steht.** Der Fall
  „derselbe Bearbeiter, nächster Tag" spart sich damit das Tippen; ein bereits eingetragener
  Name wird nie stillschweigend überschrieben.
- **D-161 · 2026-08-30 · UI-Strom · Der Berichtstext ist eine reine Funktion** (`describeImport`), nicht Text in
  der Komponente. Grund: der Wortlaut steht damit im Test, und Dialog und Banner sagen
  dasselbe.
- **D-162 · 2026-08-30 · UI-Strom · Die Tabelle ist virtualisierungsfähig geschnitten – ohne Virtualisierung.**
  Aufgabe 5b, Punkt 3. Fünf Dinge: (1) Der Zeilentyp `FindingRow` trägt die Entscheidung
  **in den Daten**, nicht in einer Closure der Spaltendefinition – vorher baute jede
  Entscheidung alle Spalten und damit die ganze Tabelle neu; `COLUMNS` ist jetzt konstant.
  `finding.decision` reicht dafür nicht, dort steht schemakonform keine `action`.
  (2) `ROW_HEIGHT = 56` als Konstante: jede Zeile genau zwei Textzeilen hoch, die spätere
  `estimateSize`. (3) Eigener begrenzter Scrollbereich mit klebendem Kopf statt einer
  scrollenden Seite. (4) Zeilenzugriff über `scrollToIndex(index)` und `indexOfId` statt
  `querySelector('[data-selected]')` – mit Virtualisierung ist die gewählte Zeile womöglich
  gar nicht gerendert. (5) `FindingsTableRow` mit `memo`; das greift, weil TanStack das
  Zeilenmodell an `table.options.data` bindet (`createCoreRowModel`, `memoDeps`) und die
  Zeilenobjekte damit stabil bleiben, solange die sichtbare Liste dieselbe ist. Ein Fenster
  über 10.000 Findings ersetzt danach nur die Schleife in `FindingsTable`.
- **D-163 · 2026-08-30 · UI-Strom · Kein `@tanstack/react-virtual` installiert.** Der Nachrüstpunkt steht, die
  Abhängigkeit nicht – sie bräuchte einen eigenen Eintrag hier, und die sechs Beispiele
  brauchen sie nicht. Sechs Zeilen zu virtualisieren wäre Aufwand ohne Wirkung.
- **D-164 · 2026-08-30 · UI-Strom · `src/components/ui/table.tsx` nimmt `containerClassName` und `containerRef`.**
  Zweite Anpassung an einer shadcn-Primitive nach `sheet.tsx`. Der Container ist der
  scrollende Vorfahr, an dem `position: sticky` und später der Virtualizer hängen; ohne die
  beiden Props ließe er sich von außen weder begrenzen noch messen. Verworfen: einen eigenen
  Scroll-Wrapper darum legen – dann gäbe es zwei ineinander liegende Scrollbereiche, und
  `sticky` klebte am falschen.
- **D-165 · 2026-08-30 · UI-Strom · Der App-Rahmen ist fensterhoch und scrollt nicht.** `AppShell` ist `h-svh`
  mit `overflow-hidden`, der Inhalt eine Flex-Spalte mit `min-h-0`; der Explorer füllt sie und
  seine Tabelle scrollt in sich. **Folge für Aufgabe 6:** eine Ansicht, die selbst lang wird
  (Dashboard), bringt ihr eigenes `overflow-y-auto` mit. Das ist die einzige sichtbare
  Änderung der Aufgabe 5b; Freigabe Victor.
- **D-166 · 2026-08-30 · UI-Strom · Die Euro-Wirkung wird je Währung summiert, nie umgerechnet.** Freigabe
  Victor. Aufgabe 6. `summarize` gibt `totals: MoneyTotal[]` zurück, nicht einen Betrag;
  mit den sechs Beispielen ist das genau eine Zeile („73.042,40 €"). Ein Kurs stünde in
  keinem Feld des Findings, und eine Summe ohne Währung ist keine Zahl (CLAUDE.md, Regel 2).
  Verworfen: über `impact_eur.amount` hinwegsummieren und die Währung der ersten Zeile
  drüberschreiben.
- **D-167 · 2026-08-30 · UI-Strom · Die Top-Liste enthält nur Findings mit `impact_eur`.** Freigabe Victor.
  `impactCents` zählt ein fehlendes `impact_eur` als 0 – in einer Rangliste „nach
  Euro-Wirkung" stünde damit ein Finding mit „0,00 €", dem keine Regel einen Betrag
  zugeschrieben hat. Die übrigen werden unter der Liste gezählt („2 Findings ohne
  Euro-Wirkung"), statt still zu verschwinden (Regel 4). Dieselbe Regel in der
  Kategorie-Kachel: eine Kategorie ohne Betrag bekommt „keine Euro-Wirkung", keine Null.
- **D-168 · 2026-08-30 · UI-Strom · Der Sprung aus dem Dashboard ist ein Reducer-Schritt (`focus_finding`).**
  Freigabe Victor. Tab, Filter, Suche, Auswahl und Drawer in einer Aktion: sonst gehörte die
  offene Karte zu einer Zeile, die die Liste dahinter gerade wegfiltert – `J` liefe ins Leere
  und die Auswahl verschwände beim nächsten Filterwechsel. War eine Filterung eingestellt,
  trägt der Zustand `filtersResetNotice` und der Explorer zeigt eine dezente Zeile „Filter
  zurückgesetzt" (Zusatz Victor); sie räumt sich nach 6 Sekunden selbst weg und bei jeder
  Änderung an Tab, Filter oder Suche ohnehin. Verworfen: ein zweiter `ReviewDrawer` im
  Dashboard – dort gäbe es keine Liste, durch die `J`/`K` laufen könnten.
- **D-169 · 2026-08-30 · UI-Strom · Das Dashboard rechnet über den ganzen Lauf, die Entscheidungen der Sitzung
  zählen mit.** Freigabe Victor. Filter gehören zur Suche, nicht zur Lage – die Kacheln
  beschreiben den Datenstand und ändern sich nicht, wenn im Explorer ein Filter steht. Die
  Entscheidungen liegen dagegen schon als Überlagerung über den Findings (`applyDecisions`
  in `App`), also fällt „davon offen" sofort, auch ohne Export.
- **D-170 · 2026-08-30 · UI-Strom · Die Score-Kachel nennt keine Zahl.** Sie steht mit gestricheltem Rand und
  dem Text „ab Sprint 4" da (so die Spec); der Rauchtest prüft ausdrücklich, dass in ihrem
  Abschnitt kein Zahlenknoten steht. Grund: der Score ist die eine Kennzahl auf diesem
  Bildschirm, die niemand nachrechnen kann – ein Platzhalterwert würde geglaubt.
- **D-171 · 2026-08-30 · UI-Strom · Der Anteil je Stufe steht als Bruch („3 von 6"), nicht als Prozentzahl.**
  Bei sechs Findings ist „50 %" eine gerundete Behauptung über drei Stück. Alle vier
  Stufen stehen da, auch die leere: dass es keine Stufe A gibt, ist die Aussage.
  **Nachtrag (Aufgabe 7, Freigabe Victor):** Der Balken daneben ist gestrichen. Bei vier
  Zeilen mit zweistelligen Zahlen trug er nichts, was der Bruch nicht schon sagt, und ein
  gerundetes Bild neben einer exakten Zahl lädt zum Ablesen des Bildes ein.
- **D-172 · 2026-08-30 · UI-Strom · Kategorien werden Währung für Währung verglichen, in der Reihenfolge des
  Laufs.** Bei einer Währung ist das schlicht „nach Betrag absteigend"; bei mehreren bleibt
  es eine feste Reihenfolge, ohne einen Kurs zu erfinden. Gleichstand geht nach
  Kategoriename, damit nichts der Einfügereihenfolge einer `Map` überlassen bleibt (Regel 9).
- **D-173 · 2026-08-30 · UI-Strom · Die Stichproben-Freigabe setzt `in_progress`, nicht `done`.** Freigabe
  Victor; die Spec-Stelle in `docs/specs/SPRINT-5-UI.md` (Aufgabe 7: „alle Findings der
  Gruppe auf `done`") gilt damit als überholt und ist beim Merge dort zu korrigieren.
  Grund: `done` hieße „in SAP umgesetzt", und das weiß das UI nicht – es weiß nur, dass
  entschieden wurde. `done` vergibt der nächste Lauf, in dem das Finding nicht mehr
  auftaucht (dieselbe Regel wie bei der Einzelentscheidung seit Aufgabe 3). Verworfen:
  eine vierte Aktion `release` mit eigenem Status – zwei Wege zu „übernommen" wären eine
  Unterscheidung ohne Unterschied.
- **D-174 · 2026-08-30 · UI-Strom · Eine Ablehnung in der Stichprobe sperrt die Gruppenfreigabe für diesen
  Lauf endgültig.** Freigabe Victor. Wenn die Regel schon an einem von zehn geprüften
  Fällen falsch liegt, ist die Grundlage für „die anderen 230 stimmen auch" weg. Die
  übrigen Findings bleiben einzeln entscheidbar, und die Sperre sagt das ausdrücklich –
  sie nimmt Arbeit weg, keine Möglichkeiten. Der Ausgang steht in `decisions.json`
  (`sample_reviewed`), sonst wäre „endgültig" nur bis zum Feierabend wahr. Verworfen:
  eine neue Stichprobe nach einer Ablehnung ziehen zu lassen – das ist Würfeln, bis es
  passt.
- **D-175 · 2026-08-30 · UI-Strom · Die Stichprobe wird aus den offenen, freigebbaren Findings der Gruppe
  gezogen, mit Seed aus `run_id` und `rule_id`.** Gleicher Lauf und gleiche offene Menge
  ergeben dieselbe Auswahl (Regel 9, kein `Math.random`); gemischt wird über die nach
  `finding_id` sortierte Liste, damit die Sortierung der Tabelle die Auswahl nicht
  verschiebt. Schadensklasse 1 ist weder Kandidat noch Ziel einer Freigabe (Regel 11) und
  wird an der Gruppe genannt, nicht verschwiegen. Verworfen: aus allen Findings der Gruppe
  zu ziehen – dann stünden schon entschiedene Fälle in der Stichprobe und eine alte
  Ablehnung sperrte die Gruppe, ohne dass jemand etwas getan hat.
- **D-176 · 2026-08-30 · UI-Strom · Die Bereinigungsliste enthält nur die übernommenen Massenänderungen.**
  Sie ist eine Arbeitsanweisung fürs SAP-Team, keine Bestandsaufnahme des Laufs; wer
  nichts entschieden hat, bekommt keine Zeile. Freigabe Victor. Format: Trenner `;`,
  CRLF, UTF-8 **mit** BOM (ohne BOM zerlegt Excel die Umlaute), Felder mit `;` oder `"`
  nach RFC 4180 gequotet, Reihenfolge Regel → Geschäftspartner → `finding_id`. In den
  Spalten stehen Schlüssel und Feldwerte, keine Namen oder Adressen. Verworfen: TSV
  (unsichtbarer Trenner) und CSV ohne BOM (Umlaute kaputt beim Doppelklick).
- **D-177 · 2026-08-30 · UI-Strom · Stichproben liegen in einem eigenen Reducer neben den Entscheidungen.**
  `state/samples.ts` statt einer erweiterten `DecisionsState`. Grund: eine Freigabe
  *schreibt* Entscheidungen, ist aber selbst keine – und die Sperre nach einer Ablehnung
  ließe sich aus den Entscheidungen gar nicht ablesen, weil sie keine erzeugt. Verworfen:
  beides in einen zusammengesetzten Zustand zu ziehen; das hätte jede Stelle angefasst,
  die heute mit `Record<finding_id, DecisionRecord>` arbeitet.
- **D-178 · 2026-08-31 · UI-Strom · `src/types/finding.ts` steht auf Schema 1.1; Quelle ist die Schemadatei.**
  Nachgezogen wurden alle Felder aus D-069: `evidence[].reference_kind` (Enum aus acht
  Werten, jetzt als `REFERENCE_KINDS` mit im Driftschutz), die Beleg-Erweiterungen
  `reference`/`document_date`/`cleared_on`/`amount`/`currency`, `entity.records[]`
  (`{bp_key, fields}`), `proposed.golden_record` und `decision.assigned_to` – alle
  optional wie im Schema. `GoldenRecord` ist als `{ [F in keyof RecordFields]?:
  GoldenRecordEntry }` geschrieben statt als zweite Feldliste: die beiden Listen dürfen
  nicht auseinanderlaufen (D-069, Punkt 6), und ein Mapped Type macht daraus eine
  Zusage des Compilers statt eine Absprache. Verworfen: die elf Felder ein zweites Mal
  aufzuzählen.
- **D-179 · 2026-08-31 · UI-Strom · `relevance` heißt `open_items`/`volume_12m` + `currency`, und die Karte
  zeigt die Währung aus dem Finding.** Die alten Namen `open_items_eur`/`volume_12m_eur`
  sind weg (D-069, Punkt 5: der Währungsname steckt seit D-030 nicht mehr im Feldnamen).
  `Relevance.tsx` formatiert deshalb über `formatMoney(betrag, relevance.currency)` statt
  über `formatEur` – die Beträge stehen in der Hauswährung des Buchungskreises und sind
  nicht umgerechnet; ein festes Eurozeichen hätte bei einem Mandanten mit CHF-Buchungskreis
  einen falschen Betrag behauptet (Regel 2). `currency` allein lässt den Abschnitt weiter
  verschwinden: die Währung ist Pflicht, sobald der Block dasteht, und trägt für sich
  genommen keine Aussage.
- **D-180 · 2026-08-31 · UI-Strom · `title` ist Pflicht – auch an der Ladegrenze, nicht nur im Typ.**
  Das Schema führt `title` unter `required` (D-069, Punkt 5); `finding.ts` sagt jetzt
  `title: string` statt `title?: string`. Damit der Typ nicht über die Daten lügt, prüft
  `checkFinding` in `@/sources/parse` das Feld mit, und die drei Notbehelfe im UI
  (`title ?? '—'`, `title ?? rule_id`, `title ?? ''`) sind ersatzlos weg. Grund: eine
  Datei ohne `title` ist gegen Schema 1.1 ungültig, und eine leere Zelle wäre genau das
  stille Verschlucken, das Regel 4 verbietet. Verworfen: den Typ optional zu lassen und
  die Fallbacks stehenzulassen – dann bleibt offen, ob eine leere Titelspalte ein Datenoder
  ein Anzeigefehler ist.
- **D-181 · 2026-08-31 · UI-Strom · Der Lauf-Kopf kommt aus `run.json`, nicht aus den Findings.** Bis Sprint 3
  gab es nur `findings.json`, und `deriveRun` hat den Kopf daraus abgeleitet. Seit
  `mdq run` liegt `runs/<run_id>/run.json` daneben und ist die Quelle: `tables_loaded` ist
  dort echt, und `company_codes` nennt die Buchungskreise **des Laufs** – einschließlich
  derer ohne Befund, die aus den Findings gar nicht ableitbar sind. „Findings-Datei laden"
  nimmt deshalb jetzt mehrere Dateien entgegen; unterschieden werden sie am Inhalt
  (Liste bzw. `{findings: […]}` gegen Objekt mit `run_id`), nicht am Dateinamen, damit
  ein umbenannter Export trotzdem richtig landet. `deriveRun` bleibt als Notbehelf für
  „nur `findings.json` zur Hand", dann weiterhin mit `tables_loaded: 0`. Passt die `run_id`
  der beiden Dateien nicht zusammen, ist das ein Fehler mit Meldung, keine Mischung
  (Regel 4). Verworfen: die Dateien am Namen zu erkennen; verworfen auch, `run.json` zur
  Pflicht zu machen – eine einzelne Findings-Datei bleibt ansehbar.
- **D-182 · 2026-08-31 · UI-Strom · `scripts/build-data.mjs` ist ausdrücklich der Ersatz für die sechs
  Beispiel-Findings, kein Lauf.** `tables_loaded: 0` steht dort jetzt kommentiert für
  „hier lief keine Engine, es wurde keine Tabelle geladen"; das Banner blendet die Angabe
  bei 0 aus, statt eine Zahl zu erfinden. Ebenso sind die `company_codes` dort eine
  Ableitung aus sechs Beispielen und kein Laufumfang. Das Skript bleibt, weil die
  Gestaltung und die Tests auch ohne Engine-Lauf etwas zum Anzeigen brauchen.
- **D-183 · 2026-08-31 · UI-Strom · Der Driftschutz löst `$ref` auf.** Schema 1.1 hat die Wiederholungen
  nach `$defs` gezogen (D-069, Punkt 7); der Enum-Test lief danach in „Pfad im Schema
  nicht gefunden" – ein Schema-Umbau, gemeldet als fehlendes Feld. `at()` folgt jetzt
  `{"$ref": "#/$defs/…"}` (mit Kreis-Erkennung), und dazugekommen sind drei Proben:
  `version` ist `"1.1"`, `title` steht unter `required`, und `entity.records[].fields`
  und `proposed.golden_record` führen dieselben Feldnamen. Verworfen: die Enum-Liste im
  Test wörtlich zu wiederholen – dann prüft der Test sich selbst.
- **D-184 · 2026-08-31 · UI-Strom · Spec-Korrektur: die Gruppenfreigabe setzt `in_progress`, nicht `done`.**
  `docs/specs/SPRINT-5-UI.md`, Aufgabe 7 stand auf `done`; der Code setzt seit Aufgabe 7
  `in_progress`. Korrigiert wurde die Spec, nicht der Code: die Freigabe ist eine
  Entscheidung, keine Umsetzung – `done` gehört an die Rückmeldung aus SAP, nicht an den
  Klick im UI. Dieselbe Begründung trägt schon die Einzelentscheidung „Übernehmen".
- **D-185 · 2026-08-31 · `entity.display_name` füllt der Executor zentral aus `business_partner`, nicht jede Regel für sich.** Rückmeldung des UI-Stroms aus der Handprobe am Lauf `2026-08-28-702323b8`: das Feld war in allen Findings leer, Liste und Top-10 zeigten „—". Es ist für den Betrieb gedacht – wer ein Finding bearbeitet, braucht mehr als `C:0000100234` –, und genau deshalb gehört es an eine Stelle: 14 Regeln, die dieselbe Zeile selbst zusammensetzen, liefern 14 Schreibweisen. Format wie in `logic/examples/findings/`: `"<NAME1>, <ORT>"` („Müller Maschinenbau GmbH, Augsburg"). Fehlt eines der beiden Felder, steht das andere allein; fehlen beide, bleibt das Feld weg – ein Komma ohne Namen ist kein Name (das Schema erlaubt beides, `["string","null"]` und optional). Der Name geht **nicht** in `ID_COLUMNS` ein: eine Umbenennung im Stammsatz darf die `finding_id` nicht verschieben, sonst wäre jede getroffene Entscheidung nach dem nächsten Namenswechsel verwaist (D-027, Regel 9). Und er bleibt auf `findings.json` beschränkt – Regel 8 gilt unverändert für Report, Hinweise und Logs. Der Regel-8-Test prüft das jetzt schärfer als vorher: statt gegen eine Handvoll erfundener Namen läuft er gegen die Namen, die der Lauf **tatsächlich** in seine Findings geschrieben hat, und sucht jeden davon in `report.txt` und `run.json`. Verworfen: `display_name` je Regel im SQL (14 Schreibweisen, und jede Regel müsste `business_partner` mitjoinen, auch wo sie es sonst nicht braucht), das Feld ersatzlos streichen (die Bearbeiterin arbeitet dann mit Schlüsseln allein), den Namen in die `finding_id` aufzunehmen.
- **D-186 · 2026-08-31 · Ohne Soll kein `proposed` – und ein `proposed`, das nur eine Quellenlage trägt, bricht den Lauf ab.** Entschieden von Victor. Vier Regeln lieferten ein `proposed`, dessen `value` `null` war und dessen Text nur erklärte, warum es kein Soll gibt („Kein Soll ableitbar: 10 Rechnungen verteilen sich auf 4 Zahlungsbedingungen", „Richtige Nummer über VIES bestätigen lassen"). In jeder Ansicht sieht das aus wie ein leerer Vorschlag; das Schema erlaubt, `proposed` ganz wegzulassen („null bei Stufe C/decision ohne Vorschlag"). Ab jetzt: **kein Soll → kein `proposed`**. Der erklärende Satz wandert dorthin, wo er hingehört – das Vorgehen nach `remediation.steps`, der Grund für das fehlende Soll nach `why` oder `if_wrong`, die Verteilung der Belege in `current.display`. Bereinigt: AR-COM-002 (nur die Stufe-C-Zeilen; Stufe B behält ihr Soll, das Schema verlangt es dort), AR-VAL-002, AR-VAL-003 und – dieselbe Form, im selben Zug statt inkonsistent stehen zu lassen – AR-VAL-005; dazu das Beispiel-Finding F-006 (`F-e2f7b19c4d83`), dessen Handlungssatz wörtlich schon als `remediation`-Schritt dastand und dessen Quellenlage in den zwei Evidenz-Einträgen steht. **Der Executor-Schutz in seiner tragfähigen Fassung:** ein Soll ist ein Wert (`proposed_value`), eine aufbereitete Entscheidung (`options`) **oder** eine Handlung, die in kein Feld passt (`proposed_display`); trägt `proposed` nichts davon und nur eine `source_summary`, ist es ein `InvalidFindingError` und der Lauf endet mit Exit 2 (Regel 6, D-098). Die wörtliche Lesart „`proposed` ohne `value` ist ein Fehler" ließ sich nicht umsetzen, ohne bestehende, freigegebene Regeln zu brechen: **AP-LEA-001** ist Stufe B und trägt „Rückforderung oder Verrechnung" – das Schema verlangt bei Stufe A/B ein `proposed`, und ein Handlungssatz *ist* dort das Soll (so auch F-003 und F-005, die Victors Spec sind); **AR-CON-002** und **AR-HYG-001** tragen `options` ohne Wert, was D-111 ausdrücklich so festgelegt hat. Der Schutz greift damit genau die Form, die gemeint war: den leeren Text. Folge für den UI-Strom: der Fall „nur `proposed.display`" (dort „Empfehlung") kommt weiterhin vor, aber nicht mehr aus F-006 – der Rauchtest hängt jetzt an F-005, und ein zweiter Test deckt den neuen Fall „gar kein `proposed`" ab. Regression unverändert 0/0/0. Verworfen: den Erklärsatz in `proposed.display` stehen zu lassen (er behauptet ein Soll), ihn ersatzlos zu streichen (Regel 4: die Verteilung der Belege ist eine Auskunft, die der Bearbeiterin fehlt).
- **D-187 · 2026-08-31 · Ein Betrag im Freitext wird an genau einer Stelle geschrieben – deutsch, mit Währung daneben, ohne Locale.** Rückmeldung des UI-Stroms aus der Handprobe: in der Findingliste stand „Mögliche Doppelzahlung: 42100.00 EUR" und daneben, aus `impact_eur` formatiert, „42.100,00 €" – derselbe Betrag in zwei Schreibweisen auf derselben Zeile. Der Titel ist Freitext aus dem Regelkopf, und die UI formatiert ihn zu Recht nicht: ein Betrag mitten im Satz ist von der Prosa nur durch Raten zu trennen. Also wird er schon geschrieben, wenn er entsteht. Die Funktion gibt es zweimal, weil Text an zwei Orten entsteht: `engine/mdq/formats.py:format_amount` für den Run-Report und das SQL-Makro `mdq_money(amount, currency)` in `logic/schema/rule_macros.sql` für Regeltitel und `source_summary`. Beide gruppieren rein über Zeichenketten – kein `locale`, kein `float`, kein Runden (Regel 2 und 9): `42100.00, 'EUR'` → `42.100,00 EUR`, `-1092.32` → `-1.092,32 EUR`, `NULL` bleibt `NULL`, mehr als zwei Nachkommastellen sind ein Fehler mit Namen. Dass die beiden nicht auseinanderlaufen, hält ein Äquivalenztest über zwanzig Grenzwerte fest – dasselbe Muster wie beim Staging-Makro gegen `formats.py`. Das Makro liegt **nicht** im kanonischen Schema: es ist eine Darstellungshilfe und keine Tabelle, und `canonical.sql` wird von `parse_schema` gelesen. Es kommt über `ensure_rule_macros()` in jede Verbindung, in der eine Regel läuft. Nachgezogen: AP-LEA-001 (Titel, `current.display`, `params.amount`), AR-CON-002 (Titel und `source_summary`), AR-HYG-001 (`source_summary`, wo `0,00` als Literal stand), und die Summenspalte in `report.txt` – dort steht die Währung in einer eigenen Spalte und deshalb nicht noch einmal am Betrag. **`expected_findings.yaml` vergleicht keine Titel** (`REQUIRED_FIELDS` + `OPTIONAL_FIELDS` in `regression.py`: `rule_id`, `bp_key`, `defect`, `company_code`, `document_no`, `finding_key`, `from_rule_version`); die von Victor vorsorglich freigegebene Erwartungsänderung war deshalb nicht nötig, und **kein Eintrag wurde angefasst**. Regression unverändert 0/0/0. Verworfen: `format` bzw. `printf` in DuckDB (schleust den Betrag durch ein `double`), `locale`-abhängige Formatierung (derselbe Lauf sähe auf zwei Rechnern verschieden aus), den Betrag im Titel wegzulassen (er ist die Aussage des Findings – „Doppelzahlung" ohne Summe ist eine halbe Nachricht).
